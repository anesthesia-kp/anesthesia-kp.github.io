# START HERE — KP East Bay Anesthesia. Both sites. The ONLY document you paste.
<!-- answers: how we work — every binding rule, once; the map of every file and archive; the LIVE line — the one file the owner pastes; read first, every session, then TODO's STATUS block and §1 -->

**LAST REVISED: 9 Sep 2026** — the docs pass (DECISIONS §188): this file is rules and a map; state lives in `TODO.md`'s STATUS block and §1; every story behind a rule is in `START-HERE-ARCHIVE.md`.

**LIVE, verified cache-busted TWICE: auction admin 329 · staff (index) 169 · mobile 18 · schedule admin 151 / staff 51** (329 = `b0ed735`, §187; the next auction honesty baseline is that SHA, staff 169). `node status.mjs` checks this line against `versions.json` and exits non-zero when it drifts.

**FILED, NOT YET PUSHED: auction admin 330 / staff 170 + `firestore.rules` (§189, 9 Sep 2026)** — User Activity page, KP e-mail Disable, the two activity logs. His push, his console paste, his RA-2. Delete this line and bring the LIVE line to 330 / 170 once both are served (verified twice).

**⛔ THE AUCTION CODE IS CLOSED BY §92: NO CHANGE TO ANYTHING `vacation-kp.github.io` SERVES — pages, `versions.json`, `firestore.rules` — WITHOUT A SPECIFIC DECISION FROM THE OWNER FOR THAT CHANGE.** §164 governs every build that follows: narrow, safe, winding down. The SCHEDULE is PARKED (§143, §157). What is outstanding, what is closed and must never be re-raised, and the held schedule pile are all in `TODO.md` §1.

## THE MAP — generated by `node status.mjs` from each file's own line-2 `<!-- answers: … -->` note. A file with no note fails the gate.
<!-- MAP:BEGIN — generated by status.mjs from each file's own "answers" note. DO NOT EDIT BY HAND. -->
| file | answers | lines | last changed |
|---|---|---|---|
| `anesthesia-kp.github.io/START-HERE.md` | how we work — every binding rule, once; the map of every file and archive; the LIVE line — the one file the owner pastes; read first, every session, then TODO's STATUS block and §1 | 166 | today (uncommitted) |
| `anesthesia-kp.github.io/TODO.md` | what is OUTSTANDING on both sites — the generated STATUS block, the §1 queue and its standing constraint, the held schedule pile, and the CLOSED / DECLINED never-re-raise table; read at §4 step 1 of every session | 225 | today (uncommitted) |
| `anesthesia-kp.github.io/HANDOFF.md` | how a session closes (the checklist), how the files are maintained, the standing traps of the tools and machines, PART A–D reference and D1–D5, and the latest session entries; read the checklist before any hand-over | 827 | today (uncommitted) |
| `anesthesia-kp.github.io/DECISIONS.md` | every owner ruling on both sites, numbered and dated, never trimmed — the generated index (§ · date · title · status) first, then the body; read the index to find a ruling, read its § before re-litigating or building on it | 5043 | today (uncommitted) |
| `anesthesia-kp.github.io/HANDOFF-ARCHIVE.md` | sections archived from HANDOFF.md, verbatim and dated (moved by archive.mjs) — read when a working file points here, or when a fact is absent from the working file | 1522 | 2026-09-08 |
| `anesthesia-kp.github.io/START-HERE-ARCHIVE.md` | the stories behind START-HERE's rules and every paragraph retired from it, including the whole pre-pass START-HERE of 7 Sep 2026 — why a rule exists, by rule number; read when a rule's reason or history matters | 816 | 2026-09-08 |
| `anesthesia-kp.github.io/TODO-ARCHIVE.md` | sections archived from TODO.md, verbatim and dated (moved by archive.mjs) — read when a working file points here, or when a fact is absent from the working file | 1699 | 2026-09-08 |
| `tests/docs/CODE-REVIEW-FINDINGS.md` | the skeptical code review of the auction (25 Jul / 7 Aug 2026) — the full fix sketches behind TODO's M1 / L2 / L3 / L4 residuals; read when one of those residuals is touched | 119 | 2026-09-08 |
| `tests/docs/DOCS-PASS-CHECK.md` | the twelve retrieval questions that prove START-HERE's map is complete — each with its answer and the path a fresh-eyes reader used; re-run after any big archiving and record the result here | 36 | 2026-09-08 |
| `tests/docs/DOCS-PASS-PLAN-2026-09-07.md` | the approved plan for the 8 Sep 2026 docs pass (§188) — what moved where and why, the lossless guard, the self-updating map; read before changing status.mjs, archive.mjs or an archive file | 138 | 2026-09-08 |
| `tests/docs/FUZZ-summer-floor-2026-09-03.md` | the strategic-bidder fuzz on the real engine (§176, PARKED) — whether the 6-bid cap lets summer go cheap; read only if the summer floor is ever unparked | 142 | 2026-09-08 |
| `tests/docs/GO-LIVE-RUNBOOK.md` | the live-run sheet for the admin — when this happens do this, the habits, the backups; read before or during the real auction | 114 | 2026-09-08 |
| `tests/docs/RA-10-2026-09-05.md` | THE CURRENT AUDIT — RA-10 (5 Sep 2026), the adversarial audit of the live holds build 327/169: findings F1–F15, the mutation results, what was built (328, §183) and declined (§184); read before any auction build | 219 | 2026-09-08 |
| `tests/docs/RA-10-BRIEF-2026-09-05.md` | the brief RA-10 ran from (§182) — scope, lanes, the md5s of the audited bytes; read to repeat an audit of that shape | 37 | 2026-09-08 |
| `tests/docs/RA-3-INTERIM-2026-08-20.md` | RA-3 interim (20 Aug 2026) — the rules-hardening findings (changesArchive, welcomeLog readable by anyone) and the honesty notes; read for the origin of START-HERE §1 fact (4) | 329 | 2026-09-08 |
| `tests/docs/RA-4-2026-08-21.md` | RA-4 (21 Aug 2026), the narrow-scope audit — the H-/M- findings, the refuted ones, and what §86 / §87 ruled on them; read when an August H- or M- id is cited | 281 | 2026-09-08 |
| `tests/docs/RA-5-2026-08-24.md` | RA-5 (24 Aug 2026), the final pre-launch audit, CRITICAL / HIGH only, on staff 164 / admin 304; read for what was judged launch-safe and why | 145 | 2026-09-08 |
| `tests/docs/RA-6-2026-08-28.md` | RA-6 (28 Aug 2026), the private audit of the Daily Schedule — 76+ findings (E- N- R- D- S- B- V-) and the §7b proposals; read when a schedule finding id is cited or the schedule is unparked | 658 | 2026-09-08 |
| `tests/docs/RA-7-2026-09-01.md` | RA-7 (1 Sep 2026), the adversarial audit of the Draw→Under Review merge and the record-of-bidding fixes; read for the F- findings behind builds 309–311 | 262 | 2026-09-08 |
| `tests/docs/RA-8-2026-09-01.md` | RA-8 (1 Sep 2026), the audit of auction builds 306–318 — engine, merge, reports, capacity, rounds-as-phases (R-/P-/M- findings, built as 319/321/322 or left as his call); read when a rounds finding is cited | 193 | 2026-09-08 |
| `tests/docs/REHEARSAL-2026-09-07.md` | the final Chrome-driven rehearsal before go-live (§186, 7 Sep 2026) — every step with times, and the six notes O-A…O-F; read before repeating a rehearsal or fixing O-A…O-F | 107 | 2026-09-08 |
| `tests/docs/REHEARSAL-SCRIPT.md` | the owner's own ~45-minute rehearsal script (§169 / §175) — page and button names in order; read to run or update a rehearsal | 57 | 2026-09-08 |
| `tests/docs/TRACE-same-bid-2026-09-02.md` | the read-only trace of the "same bid, same answer" dialog and the Phase-4 bid-number lifecycle (§173, 2 Sep 2026); read when denial, round or bid-number rules are questioned | 88 | 2026-09-08 |
| `vacation-kp.github.io/BUILD-LOG.md` | what shipped on the Vacation Auction — one row per build with its gates and its commit; read for any auction build number's record | 93 | today (uncommitted) |
| `schedule/BUILD-LOG.md` | what shipped on the Daily Schedule — one row per build with its gates and its commit; read for any schedule build number's record | 175 | 2026-08-30 |
<!-- MAP:END -->

**How to look something up, by kind** — and the rule that governs all of it: **ABSENCE IN A WORKING FILE IS EVIDENCE OF NOTHING. Grep the archive before concluding something was never written.**
· a ruling by number or topic → `DECISIONS.md`: grep the keyword or § in the index at the top, then read that § in the body
· what shipped, and its gates → the `BUILD-LOG.md` row for that build number (auction in `vacation-kp.github.io`, schedule in `schedule`)
· what happened on a day → `HANDOFF.md` (the latest entries), then `HANDOFF-ARCHIVE.md` by date or §
· why a rule exists → `START-HERE-ARCHIVE.md`: the pre-pass START-HERE is there whole, with a search-hint table at its head — grep the rule's opening words (e.g. `8. **A GATE CAN LIE`)
· closed, declined, parked → `TODO.md` §1's CLOSED / DECLINED table — one line each, with its §
· a defect's audit → the RA report named in the map (the CURRENT audit is RA-10); a day's design record → `TODO-ARCHIVE.md`
· a tool or machine trap → `HANDOFF.md` STANDING TRAPS · the closing procedure → `HANDOFF.md` checklist · the cloud battery recipe → §6 below
· anything else → `grep -rn "<term>" anesthesia-kp.github.io tests/docs`

---

## 0 · THE OPERATING PROTOCOL — the owner's standing order, 25 Aug 2026, VERBATIM

**This governs every session and every task. §3 is its specific application and never overrides it.**

> *"Act as a Principal Software Engineer. Before implementing any changes or writing a single line of code, you must follow these operational rules:*
> *1. EXPLORE FIRST: Provide a high-level overview of the relevant files and trace the logic flow step-by-step. Do not assume anything.*
> *2. PLAN MODE: Output a detailed, multi-step implementation plan. List files to be created or modified, edge cases, and potential breaking changes. Wait for my explicit approval before proceeding.*
> *3. SURGICAL EXECUTION: Make minimal, isolated, and clean modifications. Never rewrite entire files unless strictly necessary.*
> *4. UNCERTAINTY FLAG: If any part of the requirement is ambiguous or missing context, stop and ask clarifying questions instead of guessing.*
> *5. TEST-DRIVEN: Outline how we will verify the code works locally before marking the task complete."*

What each means here: **1** read the files before describing them — a claim about code not re-read sounds exactly like a verified one. **2** the plan is output and work STOPS until he says go; a "keep going" from an earlier turn is not approval for the next change, and a queue item is never a "go". **3** the admin files are large — edits are anchored, verified, and land as diffs a human can read. **4** ask; guessing has cost three-build days. **5** say how it will be verified BEFORE building, and the verification runs in-session (§6).

## 1 · PRIORITIES — the owner's ruling, 17 Aug 2026, verbatim

> *"My current #1 priority is the vacation site. Nothing can corrupt that since we are close to launch. The schedule site is months away from actual use and I can continue to build and check that as long as it doesn't disturb the vacation site."*

**The Vacation Auction is LIVE** (35 participating anesthesiologists — owner, 19 Aug; go-live is weeks away). **The Daily Schedule is a prototype**, parked. If a schedule change carries ANY risk to the auction, it does not ship; if the auction needs attention, schedule work stops. Check `TODO.md` §1's standing constraint before doing anything.

> **THE FOUR FACTS THAT GOVERN EVERYTHING.** **(1) Weeks from go-live** — the no-live-auction window is still open and still closes. **(2) Blaze pay-as-you-go** — failure mode is a bill, not an outage. **(3) App Check ENFORCED on both projects** (Firestore only, never Auth). **(4) `firestore.rules` are HARDENED and EXECUTED-TESTED** — RA-3 closed `changesArchive` and `welcomeLog` (readable by anyone before); RA-2 proves the current rules and fails on the old ones.

**Launch and the sign-in test are OFF the list at his instruction (25 Aug) — do not re-add them and do not remind him.** Everything he has declined, parked or closed is one line each in `TODO.md` §1's CLOSED / DECLINED table. Do not re-raise any of it.

## 2 · THE TWO SITES, AND WHAT THEY SHARE

**Vacation Auction** — `vacation-kp.github.io`, served at `https://anesthesia-kp.github.io/vacation/`. **Daily Schedule** — `schedule`, in build. **ONE Firebase project holds both** (`vacation-25e8e`): `vacations/*` is the auction's, `dailysched/*` the schedule's — ONE Firestore quota and ONE bill across both sites. The CRNA auction is a SEPARATE project (`crna-vacation`), **SUSPENDED 26 Aug 2026 (§118)** — its sites 404 by design; the revival recipe is `TODO.md` CRNA-SUSPENDED. The schedule would carry 19–25 listeners per page every working day once live — redo the cost arithmetic before that, not after (`TODO.md` FB-3). `isolation-test.mjs` guards shared WRITES; nothing guards shared SPEND.

What they share — why a change to one can break the other:
- **One roster**: `vacations/userList` · `usernames` · `emails` · `loginEmails` · `emailToUser`. Both sites read them. `emailToUser` is the auction's bid-security map — getting it wrong stops real people bidding.
- **THE ONE SANCTIONED EXCEPTION**: the schedule admin's **Users page** writes that shared roster (owner ruling, DECISIONS §1); it stays LOCKED by default (§50a). Sanctioned handlers: `addSchedUser`, `saveSchedUser`, `saveSchedField`, `saveAllSchedUsers`, `removeSchedUser`, `syncEmailToUserFromLogin`.
- **FTE is SEPARATE** — `dailysched/fteMap` and the auction's FTE are different numbers for the same person, deliberately. Never sync, never fall back.
- **COST IS A SECURITY INPUT IN THE RULES.** A rules document-access is BILLED on every evaluation, so a stronger predicate on a hot document is a permanent per-delivery charge — the bid document is gated by `isVerifiedAccount`, not the stricter `isRegisteredUser`, with the residual written down. Price a predicate before strengthening it.
- **`firestore.rules` lives in the AUCTION repo** and contains the `dailysched` block. Any rules change is an auction deploy: auction discipline, published in the Firebase console, and the console publish lands BEFORE dependent client code pushes.
- **EmailJS quota is shared.** Anything the schedule ever sends draws on the auction's allowance.
- **The guard**: `tests/sched/isolation-test.mjs` fails if the schedule gains a write path to `vacations/*` outside the sanctioned handlers. Firestore rules CANNOT enforce this (same project, same signed-in person) — the code plus that test is the entire guarantee. Run it before filing anything. The auction suites also read the schedule admin page, so **the auction battery re-runs after schedule builds**.
- **A push to the auction repo is a DEPLOY of the live site**, even docs-only (Pages redeploys). Docs-only pushes are harmless only while served bytes are identical and `versions.json` is unchanged. Anything that changes served bytes deploys BETWEEN phases.

## 3 · WORKING DISCIPLINE — every binding rule, one copy. The story behind each: `START-HERE-ARCHIVE.md`, by number.

**The change itself**
1. Smallest change → explicit "go" → only that change. Never rewrite whole files. When unsure, STOP and ask. Do not agree with bad ideas — push back. **Every owner-found item outranks the queue.** (§0 rules 1–4 applied to a single change.)
2. Read → edit → `node --check` every inline script → run the suites. Extractor gotchas: no default-param braces (`opts={}`), no stray `{` in comments inside extracted functions. Prefer grep + ranged reads.
3. Every fix ships with tests that EXECUTE real extracted code, plus an honesty check proving they FAIL on the previous build. Small batches by subsystem, adversarial re-audit after each. Verify every assumption against code before acting.
4. Bump `var BUILD` AND `versions.json` together. Never deploy; never write production Firebase. **The owner does every push** (GitHub Desktop). Claude files to the working tree and byte-verifies (md5 device vs cloud). Delivered files are always also WRITTEN into the repo folders without asking.
5. No one-click destructive actions, rehearsal OR live: every auction-critical write or send passes through a dialog; dialogs are paint, GUARDS at the moment of action are the enforcement. Rehearsal Mode gates the simulator and the skip buttons; mail stays LIVE in rehearsal; every restore lands with rehearsal OFF. Duplicate login e-mails are refused at entry in BOTH admin sites and fail CLOSED in the map. Passcodes are retired forever.
6. **Never present invented data as the owner's** (§22). These repos are PUBLIC — describe defects by shape, never by reproduction. Plain language always; the owner is not a coder.
7. ⛔ **THE AUCTION CODE IS OFF LIMITS — §92.** No change to anything the auction serves without a SPECIFIC decision from the owner FOR THAT CHANGE; a general "go" on other work is not it, nor is a finding. READING it is always allowed and required: the isolation guard and the full auction battery run after schedule builds. If something auction-side looks wrong, RAISE it and stop.

**What the gates taught — each one paid for, usually twice**
8. **A gate can lie in both directions.** A SUCCESS must be shown to have actually run (the sweep that exited 0 having tested nothing; `pgrep -f` matching its own shell — check the log's mtime, never the process table); a gate that fires on the wrong thing gets ignored, and an ignored gate is worse than none; and an input that is IGNORED rather than rejected returns the same number for every build — **before believing a bisect, change the input and check the output changed.** Fix the aim, do not lower the bar.
9. **Ask of every build that adds a page or a control: what would still pass if the whole thing were INERT?** If "everything", the gate is the wrong shape. Such a build ships with something that DRIVES it — a browser suite that clicks, or at minimum the render harness. (Build 88, FAST-1.)
10. **Test the code, not the prose — an assertion that cannot fail is a false one — and prove the stripper kept the code.** A `/*` inside a LINE comment opens a block comment that ran to 70% of the schedule admin: strip line comments first, then block comments, and assert what survived. Strip comments before any textual assertion; make the search window reach the line. **Stub the SHAPE of the real function, never the convenience.**
11. **Read an exit code directly, never through a pipe** (`… | tail` reports tail's status). Make a MISSING function, DOM element or assertion-message input report a legible FAILURE rather than throw — the honesty run is where absence lives.
12. **When a suite must be edited to pass, ask which you are changing: what is TRUE, or what is ASSERTED.** Repoint an assertion whose target moved; replace one whose premise the build changed with an INVARIANCE the previous build fails. Never stub the new behaviour.
13. **The pasted governing document is a snapshot; the disk copy is the document.** Read from disk, and fetch before reading.
14. **A concession is worth no more than the claim it replaced** — *run it, don't recall it* governs Claude's agreements as much as its assertions. Before building or asking anything, **check whether the code already answers it.** Where Claude changes its position on evidence, say so and date it.
15. **Write patches that refuse to apply twice** — assert `count(old)==1` before substituting; md5 both sides after any interruption. **Verified base → ship the file; unverified base → ship the patch.**
16. **An assertion that pins a count or a quoted string decays into a no-op as the code moves. Assert the INVARIANT** — re-anchored suites come out STRICTER than what they replaced.

**The paperwork — at these moments, these files, EVERY time**

| when | what changes |
|---|---|
| a ruling / an idea / a "we should eventually…" | `DECISIONS.md` (ruling — and its index row's status word) or `TODO.md` (idea) — **IN THE SAME TURN it is said.** A chat is one compaction from gone |
| he declines something | one line in `TODO.md` §1's CLOSED / DECLINED table, with its §, the same turn |
| a build is filed | `BUILD-LOG.md` row (same breath as the code) · `COMMIT-MESSAGE.txt` per repo touched · the same summary to the chat outputs column · `TODO.md` status |
| the owner pushes | `node status.mjs` in this repo — it regenerates `TODO.md`'s STATUS block, this file's MAP and `DECISIONS.md`'s index, and **exits non-zero** when this file's LIVE line or date is stale, or a governing file is over its tripwire (this file 350 · `TODO.md` 700 · `HANDOFF.md` 900). Derived facts are never hand-typed |
| a batch completes | tick it in `TODO.md` §1 |
| session end | the CLOSING CHECKLIST at the top of `HANDOFF.md`, every step; over a tripwire → promote the lesson, then `node archive.mjs <file> "<heading>"`; the repos are the memory, not the chat |
| **THIS file is edited** | **bump `LAST REVISED` IN THE SAME TURN and re-run `node status.mjs`.** Over 350 lines is a failed gate: a rule not needed in months moves to `START-HERE-ARCHIVE.md` |

**Commit summaries — HARD CAP, owner ruling 19 Aug 2026, re-affirmed 20 Aug when he caught the drift within a day (*"i am using only the summaries though and they are too long"*): a subject line plus AT MOST 4 short lines — about 50 words. If it is longer, it is wrong. COUNT THE LINES AND WORDS BEFORE SENDING.** Say WHAT changed, not why or how it was proven — no test counts, no honesty numbers, no battery results; a wave names the wave, not the builds. Delivered THREE ways: **ONE combined `COMMIT-MESSAGES.txt`** in the chat outputs column (owner ruling 18 Aug: *"I like the 1 commit message idea, ensure that sticks"*), `<repo>/COMMIT-MESSAGE.txt` on disk per repo (never a dotfile), and the BUILD-LOG row. Any revision = the WHOLE combined file re-sent the same turn.
> ⚠️ **THE OUTPUTS FILE CONTAINS ONLY WHAT GETS PASTED** — it opens with the banner `⚠ COPY ONE SECTION PER REPO` even when ONE repo changed; dividers carry the REPO NAME and nothing else; no headings, notes or guidance of Claude's own inside it (a whole-file paste made the divider the commit subject three times: `7fcd3f1`, `594778e`, `fbea125`, `7560869`). Guidance belongs in the chat.
> ⚠️ **GitHub Desktop pre-fills the Summary on a ONE-file commit and the pasted message is lost** (owner-found, 1 Sep). When a repo's change is one file, say so in the handover and tell him to CLEAR the Summary box first.

> ⛔ **THE OUTPUTS COLUMN IS THE DELIVERABLE, NOT A COURTESY. Owner ruling, 17 Aug 2026:** *"ALWAYS ALWAYS ALWAYS send them to the output so i can easily copy."* Every time a `COMMIT-MESSAGE.txt` changes — every revision — the same text goes to the outputs column IN THE SAME TURN. A repo whose message is not in the outputs column is NOT ready to push.

> 📋 **RULES FILES GET THE SAME TREATMENT (owner rulings 18 and 20 Aug: *"I always want new firestore rules presented just like this"*):** whenever `firestore.rules` (or anything he pastes into a console) changes, deliver it to the outputs column as ITS OWN ⚙️-captioned plain-text `.txt` (`firestore-rules.txt` — never a Word doc, never a `.rules` name that opens in Word), re-sent FRESH at the moment he is ready to paste, with a caption carrying the console path (Firebase console → project vacation-25e8e → Firestore Database → Rules → select-all, paste, Publish), the line "the console validates before publishing — on any error publish NOTHING and say so", and the statement that the file is md5-IDENTICAL to the repo copy (verified, never asserted).

> 🧹 **OUTPUTS HYGIENE (owner ruling 18 Aug): the column is CURATED** — per build EXACTLY: (1) ONE combined `COMMIT-MESSAGES.txt`; (2) the rules file, ONLY when it changed; (3) ONE zip bundle (`build-<n>-files.zip`) of every other changed file, which the session commits to `_to_delete/xfer/` and UNPACKS into the repos with `device_bash` + `unzip -p <zip> <name> > <repo path>` per file (never `unzip -o` — the bridge forbids deletes), md5-verifying EVERY extracted file against its cloud copy. Never ship file bytes as base64 through `device_bash` (corrupted in transit, burns context). Copy-paste items also go in chat text.

**File hygiene** — main folders hold only what is live or in flight. Outdated → `~/Documents/GitHub/_archive/<area>/<category>/` (areas: `vacation`, `schedule`, `tests`, `anesthesia` — never invent a sibling); true machine junk → `_to_delete/` (his to empty; `status.mjs` measures it). **Nothing is ever deleted.** Grep the WHOLE GitHub folder before moving anything; if unsure, it stays. Never move what the live site serves — the hash-named HTML files are LIVE REDIRECTS; read the `<title>` first. Record every move in `_archive/README.md`. Housekeeping is its own commit; the procedure is HANDOFF's checklist step 4a. `.gitignore` does not apply to files git already tracks (`git check-ignore -v`).

## 4 · EVERY SESSION — the re-grounding ritual, ONE turn, before any work

0. **NAME THE SESSION (§185): `Vacation Auction <D Mon YYYY> V<n>`**, the date HIS (`TZ=America/Los_Angeles date`, never UTC), `n` = one more than today's dated entries in `HANDOFF.md`. First line of the first reply; the heading of the session's HANDOFF entry; he names the chat the same.
1. Read `TODO.md` — the STATUS block (it says whether any file is over its tripwire) and §1's queue and standing constraint.
2. Verify live builds: fetch both sites' `versions.json` with **`WebFetch`, NOT `curl`** (neither sandbox has egress to `*.github.io`), TWO calls per site with DIFFERENT cache-busters (it caches 15 minutes per URL). If a fetch disagrees with disk right after a push, fetch AGAIN before believing it. `status.mjs`'s live column reads "no network" on the Mac — that is why this step exists.
3. `git --no-optional-locks status --short --branch` on all four repos, and `git fetch` the three public ones (`tests` CANNOT be fetched over the bridge — private, HTTPS, no credentials — judge it from `git log` and disk and say so). Assume every build number not verified minutes ago is stale — HEAD moves under you. **Build every honesty fixture from an explicit SHA, never `HEAD~n`**; a build that was never committed has no fixture — baseline on the last PUSHED build and say so.
4. Stale-lock check before AND after the fetch: `find <repo>/.git -maxdepth 2 -name '*.lock'` — a fetch over the bridge strands `objects/maintenance.lock` in every repo it touches; `mv` any to `_to_delete/` (the bridge cannot delete).
5. Report the state in a few lines, **including the context reading (the number)**, then work. When in doubt during the day: run it, don't recall it — read from disk, md5 both sides when it matters.

**The bridge.** It drops when the Mac sleeps — *"it happens when i walk away from computer"*; mains is covered by Settings → Battery → Options, battery by Amphetamine (installed) or `caffeinate -di`; closing the lid disconnects everything. When a bridge call fails: retry ONCE, then stop and say so — and if the failed call may have WRITTEN something, check before repeating it. **Git over the bridge is read-only, always `--no-optional-locks`, and even that is not a guarantee**: never `add`/`commit`/`checkout`/`stash`/`merge`/`reset` there; a stranded `.git/index.lock` blocks GitHub Desktop. Three filesystems: the CLOUD container (`Bash`; where deliverables are built and where Chrome screenshots land), the DEVICE VM (`device_bash`; mounts only `~/Documents/GitHub`, its `/tmp` is its own), and macOS proper (unreachable). **Anything that must end up in a COMMIT is written with `device_bash` or `unzip -p`; `device_commit_files` is delivery only** — git over the bridge does not see its files as untracked. The rest of the machine-level traps are `HANDOFF.md` STANDING TRAPS.

**Context: REPORT THE READING, NEVER BLOCK ON IT (§106).** Cloud only: the last `usage` block in `/root/.claude/projects/-home-claude/$CLAUDE_CODE_SESSION_ID.jsonl`, summing `input_tokens + cache_creation + cache_read`; over the bridge it is unreadable — say UNKNOWN. His sessions have NEVER compacted below 600,000 tokens; his budget is *"stop by 700k"*. **Thresholds were invented three times and overruled three times. Report the number; do not invent a fourth.** Say so when the work is degrading (mechanical slips → asserting things about code without re-reading it → re-deriving settled questions); volunteer it, make sure the files carry everything, offer a fresh session. **A compaction is announced in the FIRST sentence of the next turn**, and the closing checklist runs BEFORE the context runs out, not after; work resumed after a compaction is done on a summary — re-ground each fact off disk before acting on it.

## 5 · ONE CHAT, BOTH SITES

Working both sites from one chat is the owner's settled preference. What makes it safe: **the repos hold the memory, not the chat** — the paperwork table is binding. **One TODO file** (owner ruling, 17 Aug): the per-repo TODO files are pointers; lists never fork again.

## 6 · RUNNING THE BATTERIES

**THE BATTERIES ARE CLAUDE'S JOB, IN-SESSION, EVERY BUILD** — his words, 17 Aug: *"yeah, i don't do those things."* Never hand him a build with a "run the battery first" instruction. `node run-all.mjs` in `tests/` (auction; discovers every `test-*.mjs`), `node sched/run-all.mjs` (schedule; a HAND-KEPT list — the runner exits 2 naming an unregistered suite). Each build's suite plus an honesty run that says **FAILED with a non-zero exit** on its explicit-SHA predecessor — **a skipped honesty check is a failed gate, not a pass**; if it says "skipped", regenerate the fixture from its SHA and re-run. Schedule suites take `PRE_ADMIN=`/`PRE_STAFF=`; auction suites `PREFIX_SRC=`; `test-audit-fixes.mjs` has a HARDCODED schedule baseline at `/mnt/user-data/uploads/GitHub/schedule/admin/index.html` — **exactly where `device_stage_files` drops the CURRENT admin page** (five auction suites have the same default for `…/vacation-kp.github.io/admin/index.html`): before believing a failure there, look at what sits at that path.
- **ONE `PREFIX_SRC` cannot satisfy every honesty block** — each was written against ITS own previous build. When no auction build changed, leave the auction baseline ABSENT: those blocks skip correctly because the battery is running as a REGRESSION gate on unchanged bytes — **say that out loud in the report.**
- **In the cloud:** both site repos are PUBLIC and clone straight in (the vacation remote is `anesthesia-kp/vacation.git`; hub and schedule by folder name); `tests` is private — tar it minus `node_modules` and stage it. `tests/` must physically SIT BESIDE the repos (`<root>/tests`, `<root>/vacation-kp.github.io`, `<root>/schedule`) with `REPO_ROOT=<root>` absolute (`~` is `/root`). `schedule` must be `--unshallow`ed — honesty suites `git show <sha>:<file>` in BOTH repos. The schedule battery also needs the `fake/` shims, `schedule/versions.json`, the auction admin page, and the global `node_modules` symlinked into `tests/node_modules/` (ESM ignores `NODE_PATH`). md5-verify the clone against the device tree before believing the run (both `index.html`s, `admin/index.html`, `crna/index.html`, `mobile.html`, `firestore.rules`). A playwright/chromium version mismatch is fixable: `PLAYWRIGHT_BROWSERS_PATH` at a WRITABLE dir of symlinks (`chrome-linux` → `chrome-linux64`; the sweep driver needs the HEADLESS SHELL binary too). The Mac cannot run the sweep (no browser, no egress). Say how many suites RAN and how many SKIPPED.
- **On the Mac:** the device has node v22 and the repos mounted — `device_bash` runs the suites directly and edits files via `python3` heredocs; it cannot delete (`mv` to `_to_delete/`); the auction battery runs in the foreground (~25 s); the schedule's browser suites SKIP there (a coverage hole, not a pass).
- **RA-2 IS THE ONE BATTERY THE OWNER RUNS** (*"I can run RA2, you built a system for me"*): the Firestore emulator jar comes from `storage.googleapis.com`, blocked in BOTH sandboxes; `tests/RA-2.command` is his double-click. Do not report the rules as untested when the answer is to ask him to run it; the record needs HIS numbers (the emulator writes `tests/rules-emu/firestore-debug.log` — its mtime is the cheap check). Save a copy of the rules before editing them; `PRE_RULES=<copy> node test-rules-emulator.mjs` is the one-change fallback. Terminal git on the Mac reads and commits fine but cannot PUSH (§144 declined fixing it).
- ⛔ **THE COST GATE (owner order, 20 Aug: *"you sabotaged me"* — two hours and ~7% of a week on a chunked-transfer fallback when a 30-second re-sign-in fixed it):** before ANY token-expensive workaround — chunked transfers, rebuilding files by hand, mass re-reads — STOP and tell the owner the cheap alternative first (re-sign-in, attach a file, push so the cloud can clone). Fallbacks are for when he is genuinely away, with the cost estimate stated first. When staging is blocked: `SendUserFile` → `device_commit_files` is byte-exact (md5 both directions); getting a file OFF the device is diff + gzip + base64 in ~12 KB chunks, verified each.

## 7 · WAIT FOR INSTRUCTIONS BETWEEN BATCHES

The owner drives the order. Present the state, propose, and stop.
