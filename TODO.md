# TODO — the ONE list, both sites

**Owner ruling, 17 Aug 2026:** one TODO file for the whole project, not four. This is it.
The per-repo `TODO.md` files are pointers here; `MASTER-TODO.md` and
`OPEN-RESIDUALS` were folded in and archived (copies in `_archive/`) the same day.

**What stays separate, and why:** `DECISIONS.md` (this repo) (the rulings register — what is
*settled*) and `schedule/BUILD-LOG.md` (what *shipped*). This file is what is *outstanding*.
A fact lives in ONE of the three, never two.

> ⚠️ **These repos are PUBLIC.** Defects are described by shape, never by reproduction.

<!-- STATUS:BEGIN — generated by status.mjs. DO NOT EDIT BY HAND. -->
**STATUS — generated 2026-08-31 16:59 UTC by `node status.mjs`. Regenerate after every push.**

| | build (disk) | versions.json | live check |
|---|---|---|---|
| Auction admin / staff / mobile | 305 / 164 / — | 305 / 164 / 18 | not checked from here (no network) — fetch live separately |
| Schedule admin / staff | 151 / 51 | 151 / 51 | not checked from here (no network) — fetch live separately |

| repo | working tree | vs origin | git locks |
|---|---|---|---|
| vacation-kp.github.io | clean | in sync with origin | no locks |
| schedule | clean | in sync with origin | no locks |
| tests | clean | in sync with origin | no locks |
| anesthesia-kp.github.io | 5 uncommitted file(s) | in sync with origin | no locks |

Suites on disk: auction **55** (`tests/test-*.mjs`) · schedule **108**
(`tests/sched/*-test.mjs`). Assertion counts come from RUNNING the batteries, never from here.

**START-HERE.md** — `LAST REVISED 31 Aug 2026`, date checked by git, read in America/Los_Angeles (edited, not yet committed — so it must say today or tomorrow) — ✅ fresh — every build number it quotes matches `versions.json`
_(the only document the owner pastes; this gate exits non-zero when it drifts)_
<!-- STATUS:END -->

---

# 1 · THE QUEUE — work, in order

## 🔴 FIRST THING NEXT SESSION — THE AUCTION BOARD IS IN AN UNVERIFIED STATE (31 Aug 2026, §145)

The Chrome-driven rehearsal was abandoned mid-flight. **Verify these three things on the live admin before
anything else, and tell him:**
1. **Was Phase 1 begun?** Claude clicked `🔒 Lock standard weeks & Begin Phase 1` and the extension died
   before the result could be read. The board may be "Not started" or may be in an open Phase 1 with zero
   bids. Either is harmless; not knowing is not.
2. **Rehearsal Mode is ON and must be OFF before any real launch — NE-10.** Reset deliberately leaves it
   armed. Nobody claimed arming it: the owner said he did the reset and did not claim the toggle. Treat the
   arming as unexplained rather than assuming.
3. **The board is otherwise empty** — he reset it himself (typed RESET): no bids, no decisions, 0 of 52
   weeks locked. His pre-rehearsal data is recoverable from the two cloud backups stamped
   *31 Aug 2026 · Phase 1 · 5 bids*.

**⚠️ DEFECT-CLASS FINDING, verified in code and in the live UI — the biggest thing this session produced:**
**the two e-mail switches do NOT gate admin-initiated sends.** `Outbid alert e-mails` is checked only where
alerts are QUEUED, and `Welcome e-mails` only in `welcomeOnce()`. Every admin blast — `📮 Send Phase Results`,
`📮 Send Round Results`, `📧 Send Reminders`, the whitelist ask — reaches `emailjs.send` through
`adminSendEmail()`, which checks no switch at all. `processMailQueue()` does not consult the switch either,
so alerts queued BEFORE it was turned off still flush. **Consequence: "I have e-mails toggled off" is not a
safe basis for a rehearsal.** The only path that completes a phase without mailing is `⏭ Skip sending
(testing)`, which exists only while Rehearsal Mode is ON. Not a live-auction bug — every send is still a
deliberate confirmed act (NE-13 holds) — but it is a trap for testing, and it is his call whether the
switches should also gate the blasts. **§92: no auction change without his specific decision.**

**The rehearsal itself is UNRUN.** It stays his final pre-launch check. Do not restart it unasked, and not
at all until the Chrome disconnections are understood (HANDOFF has the evidence).

**Smaller findings from the aborted run, all worth keeping:**
· `NEVER-EVENTS.md` NE-1 still describes the `_blockFloor` demotion engine that §77 deleted and §80 rewrote.
  The file has been stale since 20 Aug. One edit, his call.
· Whitelist tracker: **15 of 35 users have not confirmed** they whitelisted the sender — pre-launch item.
· Fair Play Monitor read **"35 users flagged (36 incidents this auction)"** on a board holding 5 bids from
  2 users. Probably the "slow starts" rule counting the 33 non-bidders, but unconfirmed. The evidence is in
  the *Phase 1 · 5 bids* backups; worth one look before launch so the real run's report is trustworthy.
· Reset's typed-token guard WORKS (pressing the final button with an empty box was refused:
  *"Not reset — you must type RESET in the box to confirm"*) — but the refusal closes the entire dialog, so a
  typo means redoing the flow including another cloud backup. Safe, clumsy. Cosmetic; his call.
· `⏭ Skip backup & continue (testing)` was correctly ABSENT from the reset flow while Rehearsal Mode was OFF
  — an NE-10 guard verified live.
· The Begin Phase 1 dialog named exactly the three high-demand weeks (Wk 47 🦃, Wk 51 🎄, Wk 52 🎆).
· Every report opens in a script-written `about:blank` tab that browser automation cannot read at all —
  screenshots, text and JS are all refused there. **Owner ruled 31 Aug he reviews reports himself.**
· There is no per-phase BID-COUNT limit configured (`maxBidsPerPhase` is unset and has no UI). Cumulative
  WEEK caps are 6 through Phase 1, 6 through Phase 2, unlimited for 3 and 4.


## 📆 THE CALENDAR FEED — RULES FILED (§136, 30 Aug afternoon). HIS FOUR STEPS, IN ORDER.

`firestore.rules`: `feedTokens` readable by the schedule admin only; `feeds/items/*` by no client; the
`vacations` block md5-identical. RA-2 has four new gates ("Feed reads (30 Aug)") and its fixture is
`28bae0c`. Auction battery on the unchanged pages: 55 suites, 2,020 assertions, 0 failed (auction honesty
blocks skipped by design — no auction page changed); isolation guard 36/36.
1. ~~Push all four repos~~ — done 30 Aug ~14:25 UTC.
2. ~~`RA-2.command`~~ — done: **146 / 146 on the published rules; honesty 4 of 4 feed gates FAILED on
   `28bae0c`** (he pasted before running it — the verdict covers the same bytes, so no harm).
3. ~~Console paste and Publish~~ — done (his word, 30 Aug ~14:28 UTC). **The §136 rules are LIVE.**
4. Terminal, in `schedule/functions`, after `npm install` (done): `npx --yes firebase-tools login`, then
   `npx --yes firebase-tools deploy --only functions:ics --project vacation-25e8e`. His first try hit
   `EACCES` on a global install and there was no `firebase.json` — both fixed the same turn
   (`functions/firebase.json`, `node_modules/` ignored, `DEPLOY.md` rewritten to the npx route). If the
   printed URL is not `https://us-central1-vacation-25e8e.cloudfunctions.net/ics`, one line
   (`FEED_URL_BASE`) changes.
Then the three-row check in `DEPLOY.md` (real link → calendar text; one character changed → 404;
`/abc` → the SAME 404). **Endpoint DEPLOYED 30 Aug ~14:35 UTC at the expected URL; `/abc` → 404 checked
from the cloud; rows 1–2 done by him ~14:50 UTC — his own link returned a 522-byte calendar after Rebuild every
feed, and a valid-shape token with no feed behind it gave the same Not found. THE FEED IS VERIFIED END TO
END. Release gate (§54) still SHUT.** Node 20 is retired by Google 30 Oct 2026 — runtime moved to 22 in
the repo the same turn and REDEPLOYED on Node 22 ~14:45 UTC (both the cloudfunctions.net and the run.app
address answer; the page's `FEED_URL_BASE` stays).

**OWNER REQUEST, 30 Aug (verbatim): *"I will need a way to send all feeds to each user's phone, probably
using gmail."*** Not yet designed or ruled. The two shapes: (a) one e-mail per doctor from the admin page
through EmailJS (the auction's provider — shared quota, §2) carrying their `webcal://` link and per-phone
instructions, sent once when the release gate opens; (b) the staff site showing each signed-in doctor their
own link (needs a per-doctor readable token document — a rules change under §92). **OWNER, 30 Aug, after comparing with QGenda (their current platform, whose phone calendar notes read "autosync
from qgenda" with a date/time): *"is that a better or worse system than we have? can we add that time stamp?"***
**RULED §140 ("unpark the auto-refresh and then do email") and ✅ BUILT AS ADMIN 149 (filed, not pushed):** feeds
rebuild themselves off `emitChanges` (debounced, linked people only), vacation deliveries diffed, one horizon read per
rebuild (was ~155 per person), the Auto-sync timestamp on every event. Row in `schedule/BUILD-LOG.md`; `build149-test`
20/20, honesty 14 red on the pushed 148. **The §137 e-mail build shipped as admin 150 — parked by §141.**

**OWNER, 31 Aug (verbatim): *"eventually, will need a different e-mail address for schedule site…
vacation goddess isn't a good one for scheduling."*** For later, with the §141 e-mail recheck: the
schedule's mail (feed links and anything after) should come from its own address, not the auction's
gmail. Means a second EmailJS service or sender identity — quota note in §2 applies. Not designed,
not scheduled — HELD with the schedule pile (§143).

**RULED §137: BOTH — THEN PARKED BY §141 (31 Aug: "e-mail will need some work down the road. not for now though").**
✅ The e-mail half is BUILT AS ADMIN 150 (pushed, live 31 Aug) — row in
`schedule/BUILD-LOG.md`; `build150-test` 34/34, honesty 27 red on the pushed 149. **RECHECK the whole flow
before any real send — his ask; the subject check below is part of it.** **⚠ ONE CHECK BEFORE THE FIRST
REAL SEND, his:** the message's SUBJECT line lives in the EmailJS template (`template_rss3fn3`, shared with the
auction's alerts) and no code controls it — press E-mail on his own row, read what arrives; if the subject is
auction-ish, a second template is two minutes in the EmailJS console and ONE constant changes in the page.
**The staff-site link half (§137 B) is NOT built:** it needs a per-doctor readable token — a §92 rules diff shown
to him first. **Deferred to a later date by §141 — no longer the next item.**

**OWNER, 30 Aug (verbatim): *"Is there a way for you paste the commit summaries into github desktop yourself?
is there an easier way so i don't have to push 3-4 repos every time? it's quite cumbersome."*** Claude cannot
drive GitHub Desktop and never commits on his Mac (the bridge strands git locks). **BUILT as `tests/PUSH-ALL.command` under §142, then
DROPPED BY HIM ON ITS FIRST RUN — §144 (31 Aug): *"forget this, i don't want to pursue this anymore."*** What the
run proved, and it is worth keeping: **Terminal git IS installed on this Mac** (the 22 Aug note in START-HERE §6 was
wrong) and the COMMIT succeeded — but the PUSH failed, because all four remotes are HTTPS and Terminal git has no
saved GitHub sign-in; GitHub Desktop carries its own, which Terminal git cannot see. The two fixes (the osxkeychain
helper, then a personal access token) were offered and declined. **The script stays on disk unused. Do not fix it,
do not re-propose it, do not raise the credential setup again.** Pushing stays GitHub Desktop, every repo, by him.

**✅ BUILT AS ADMIN 148 (pushed, live 30 Aug) — the bulk create, the calendar name `KP Anesthesia Schedule` (§137/§138), the gate as a switch, the status card. Row in `schedule/BUILD-LOG.md`; `build148-test` 33/33, honesty 29 red on 147; schedule battery 104/104 executed, all green; auction 55 suites / 2,019; isolation 36/36. Pushed.**

**OWNER-FOUND, 30 Aug (§3 rule 1 — outranks the queue): *"why create each docs 1 at a time?"*** The
Calendar Feeds page has only a per-person Create, and each then wants a Build — seventy clicks for
35 people. The per-person caution belongs to REPLACE (it breaks a live subscription), not to CREATE.
Proposed as admin 148: one "Create links for everyone without one" button (in-page dialog naming the
count, then mint + build all); Replace/Remove stay per-person and confirmed; the page's red banner
brought current (rules published, endpoint live, release gate shut). He said go; built as 148 (above). **§54's release gate stays SHUT** until he says otherwise; the token ruling (who
may regenerate one) is still his.

## 🔎 RA-6 (§124) — ADMIN 147 / STAFF 50 (§134/§135) LIVE, 30 Aug. WHAT IS LEFT IS BELOW.

**PUSHED 30 Aug (`90af11e`), served 147 / 50, verified twice cache-busted:** admin **147** — the in-page dialog for Apply, Publish and Add-the-standard-types
(Publish LISTS the standing violations; Apply the over-ceiling days; Add the types) · staff **50** — the Full
Schedule on a phone as one day per row, same page, same data (§134), and nothing on the phone scrolls sideways
(§135 — the tab strip wraps). Rows in `schedule/BUILD-LOG.md`; eight older suites re-anchored (six press the
page's OK; build132 and build125). Pushed by him; verified served 30 Aug ~14:10 UTC.

**FOR THE NEXT SESSION (he said "prepare to hand off all other tasks for a new session"):** ~~① after his push,
verify 147 / 50 served and remove START-HERE's FILED line~~ (done, 30 Aug afternoon); ② the two unbuilt §7b items below (the setup-checklist
landing page; the plan-diff Claude recommends against); ③ D-7 needs an index he creates in the console; ④ S-7 when
his request-type list is final; ⑤ B-15 is a §92 comment. Nothing is half-done: every build filed this session
has its suite, its honesty run and a green battery.


**PUSHED 30 Aug (`ac725ac`), served 146 / 49:** admin **142** (E-5, E-9, E-10, E-12, E-13, E-14, N-15, N-16) ·
**143** (R-6, R-9, R-10, R-11, R-12, D-9, D-10, D-13; the fake learned `serverTimestamp()`) · **144**
(B-8, B-9, N-20, V-5, V-6, V-8, V-10, V-15, V-18) · **145** (V-7, V-12, V-13, V-20; §7b "one collapse
control" and "one place for holidays") · **146** (§7b the sidebar regrouped) · staff **49** (D-12, D-13,
D-14, S-5/B-13, B-14, S-11, S-13, S-14/B-10, R-7, R-8, R-13/S-15, V-7's staff half). Rows in
`schedule/BUILD-LOG.md`. Twelve older suites re-anchored, never stubbed (build51, 53, 61, 64, 67, 69,
70, 80, 120, 121, 123, staff46). Pushed and verified served twice, cache-busted. No rules change; the auction untouched.

**Claude decisions to review (§131/§132) — say the word and any of them reverts in one change:**
· **The sidebar is regrouped (146):** *Schedule · Approvals · Reporting · Setup · Records* (his §133 wording) —
  every page where it was, the Engine keeps its own entry (§123 ②); the People/Admin/Setup headings
  retired. A before/after picture was sent in the chat.
· **Holidays STAY on the Shift Catalog page** (beside the staffing rules that read them, where he built
  them) and are one link from the Rules page — the §7b proposal moved them; this keeps his map intact.
· **The Reports settings and the call-baseline folds open SHUT** (§104) as the site's ▸ Show bar, and the
  "open by default while the site is being built" bar is a quiet grey line — the pages stay UNLOCKED per §60.
· **Shift Eligibility still opens on the first family** (build 67's choice) — the count line now says
  "Showing N of M shifts — filters are on" so it is never silent. Opening on all shifts is one line.
· **The cell editor gained ✕ and Escape, not a "Cancel":** every action in it applies on its own, so
  there is nothing to undo — a button named Cancel would promise one.
· **A swap denial's reason is optional** (like a request's, build 80) — inline box, never a prompt.
· **Publish tells someone whose whole month is days off** ("…1 shift and 3 days off in it"); returning
  a month to draft tells everyone in it.
· **A recolour onto a colour another family already uses ASKS** (confirm) rather than refusing.
· **S-4's duplicate month subscription was left alone** — the SDK serves a second listener on one
  document from its cache, so the read is not doubled; the fix would couple two independent
  subscriptions for no saving.
· **R-11's in-transaction guard is reordered as defence in depth** — the build-59 pre-check still
  fires first on every ordinary path; the reorder covers only the race the audit describes.

**Still open from RA-6 (Tier 2/3 and §7b), his to pick or Claude's next session:** ~~D-7~~ (✅ staff 51, §142 — ordered by `at`, capped at 150 on the server, with a fallback to the old
unordered read. HIS CONSOLE STEP IS DONE — index Enabled 31 Aug (screenshot verified: `items`, `u` ASC,
`at` DESC, scope Collection). Nothing left of D-7. The recipe, for the record: Firebase console → project vacation-25e8e → Firestore Database
→ Indexes → Composite → Add index: collection ID `items`, field `u` Ascending, field `at` Descending, query
scope Collection → Create, wait for Enabled. Until then the page quietly works the old way); **S-7** (the "specific shift" request option once types are curated) and
**B-15** (a comment in `firestore.rules` — §92) — **both LATER by §142, and HELD with the whole schedule pile
by §143: after the §142 batch, schedule work stops and the auction is next, driven by him**; **§7b:** ~~the setup-checklist landing page~~ (✅ admin 151, §142 — while most of the catalog has no
staffing rules, the coverage chip comes down and a checklist stands in its place) ·
"what a plan would do to existing assignments" (Claude recommends against — E-1 is fixed and it adds
a panel to a page just simplified). ~~In-page dialogs~~ (147) and ~~the phone layout~~ (staff 50) are done. **Already true, no build
needed:** "freeze the name column and date header" — build 132 made the first column sticky-left
and the day header sticky-top inside the scrolling box.

**Tier 2/3 NOT taken earlier (record):** ~~D-6/N-7~~ (141 and the Stage 5 rules), and every item
above that the 30 Aug wave took.

**`tests/docs/RA-6-2026-08-28.md`** (private): 76 findings raised, 74 survived an independent
adversarial verifier per report, 22 more found by the verifiers, 21 visual/UX defects, plus improvement
proposals. The earlier stages: Stage 0 (the isolation guard and runner — B-1, N-18, N-19, N-22) ·
127–130 (E-1, E-11, N-5, N-3, E-2, N-2, E-7, R-1, R-2) · 131–133 / staff 41–43 (S-2, S-12, V-4, V-9,
V-3, E-8, N-1, V-16, V-1, V-2, V-14, R-4, R-5, S-10, N-11) · 29 Aug (staff 44 Stage 3; admin 134 /
staff 45 B-3, B-4; 135 E-3, E-4/N-4; 136 D-15, D-4/N-10, D-5; 137 D-11, E-15, E-6; staff 46 S-1, S-8,
N-14; 138 §127; 139 B-2 N-20/N-21 B-7; 140 / staff 47 Stage 5's page half; staff 48 S-3 client half;
141 D-6) · **the Stage 5 rules, PUBLISHED 30 Aug** (R-3/S-3/B-5, B-6, N-8, D-1, N-6, N-12, N-13).
**Trap hit and recorded there: the first schedule run exited 0 having skipped 55 of 77 suites** (no
`playwright` module in `tests/node_modules`).


## ✅ STAGE 5 IS COMPLETE — builds 112–117, 28 Aug 2026, under §119/§120. Pushed and verified live (with 118–126) the same night.

**What shipped, one gated build each** (rows in `schedule/BUILD-LOG.md`): 112 Not on vacation +
One site per day · 113 Post-tag cooldown · 114 Not together + Only approved pairs (§7's list) ·
115 warn at the moment of change (through `assignmentWarnings`, all three paths) · 116 Fairness
balance (calls `buildModel`) + the "not checking anything" header fix · 117 quotas as
constraints in auto-populate, through ONE `fillDay` for month and year. **All seven rule types
in `design/RULES.md` exist.** Every build: browser-driven suite, honesty check FAILING on the
previous build, full schedule battery, auction battery on unchanged auction bytes.

**LATER THE SAME DAY (builds 118–124, staff 38–39, under §120 ⑦):** the built-in checks with switches (§121), the collapse control identical to the vacation site (§122), the Simulator retired and the ENGINE page under Admin (§123), draft vs published months (§5), the personal change feed (§6), atomic swaps (defect 3), in-grid rule marks (§90). **Two rules-file items are now HIS (§92):** `dailysched/changes` and the draft gate are enforced only in the pages.

**WHAT WAS LEFT OF THE SCHEDULE BEFORE THOSE (kept as the record of the morning's reading):**
draft/publish + change feed · the phone-friendly staff view (defects 17/18) · e-mail alerts ·
uniform confirmations (40 raw `confirm(` calls) · swap atomicity (defect 3) · the calendar feed's
release (his two decisions: `feedTokens` in `firestore.rules` under §92, and deploying
`schedule/functions/`). **Two Stage 5 leftovers, his call:** the rules checker sees ONE month
(a weekly cap's first week, a cooldown's last days) — reading the adjacent month is a read-cost
decision; and the every-site weekly print option (107) stays unless he narrows it.

## 🔁 (superseded by the block above) THE SCHEDULE IS BACK ON (§119, 28 Aug): *"Let's finish what's left there."*

**§119 reverses §116's direction only.** §92 still closes the auction code; §0 rule 2 is not relaxed —
each build is planned, then waits for his go. The §116 text below is kept as the record of the pause.

### (superseded) VACATION SITE FIRST, SCHEDULE ON THE BACK BURNER (§116, 26 Aug evening)

**Owner: *"move back to some work on the vacation site and put scheduling on the back burner.
don't lose track of scheduling updates still needed."*** Everything schedule-side below stays as
written — it is the record he asked not to lose — but it is not picked up unasked. **§92 is NOT
lifted by this:** auction-side changes still need his specific decision, one each. The first
vacation-side item, **his edited walkthrough deck** (§116 ③), is **DONE — owner, 28 Aug 2026:
*"Done with the ppt."*** Nothing else is queued on the vacation side.

## 🔔 PHASE-4 EXTRA FTE — AN OWNER TASK BETWEEN PHASES 3 AND 4, NOT A FEATURE (26 Aug)

His table gives 34 weeks (4/18–12/12 except Thanksgiving) **one extra FTE that opens in Phase 4**.
The site has no per-phase capacity feature — checked in code 26 Aug (`getSlots` reads one number
per week) — and he chose the manual route: *"am i correct that i will be able to add an FTE to
those weeks between phases 3 and 4? that would work fine for me."* Confirmed yes, with one
caution from the FTE panel's own dialog: **changing a week's availability re-decides that week's
winners immediately — so raise the 34 weeks only AFTER Phase 3 is completed and its results sent,
never while its bidding is open.** The deck's chart shows these as the amber tops, excluded from
the supply totals by his instruction.

## ⏸ CRNA-SUSPENDED — THE TWO CRNA SITES EXIST BUT ARE OFF THE AIR (owner, 26 Aug evening, §118)

**Addresses (keep):** staff `https://anesthesia-kp.github.io/vacation/crna/` · admin
`https://anesthesia-kp.github.io/vacation/crna/admin/` · Firebase project `crna-vacation`
(config in `vacation-kp.github.io/crna-config.json`). Both serve GitHub's bare 404 while
`"suspended": true` is set there; the landing page has no CRNA cards; reference copies of the
files as they were sit in `_archive/vacation/crna-suspended-2026-08-26/`.

**TO REVIVE (three steps, then two pushes):**
1. `crna-config.json` → `"suspended": false`.
2. `node crna-stamp.mjs` in the auction repo — regenerates `crna/index.html`, `crna/admin/index.html`
   and `crna/versions.json` from the MD pages AS THEY ARE THEN (do not restore the archived copies).
3. Landing page: restore the two `<a class="site-link" …/vacation/crna/…>` cards from
   `anesthesia-kp.github.io` commit `07b001d` (the last commit that had them) — one under the staff
   group, one under "Admin Access Only".
Then run the auction battery (`test-crna-stamp.mjs` flips back to its drift check by itself) and push
`vacation-kp.github.io` and `anesthesia-kp.github.io`. §92 applies: a revival is an auction deploy.

## ⚠️ THE SUITE LIST IN `sched/run-all.mjs` IS HAND-KEPT — three suites were never in it

**Found 25 Aug, and it is the worst kind of gap because it looks like coverage.** Builds **99,
100 and 101** each shipped with a new suite; each passed when run by hand on the day; and **none
of the three was registered in `sched/run-all.mjs`'s hardcoded `SUITES` list.** They would have
been silent for ever after, while the battery printed *"49 suites, all green"* over the top of
them. All three are registered now and the list carries a warning at its head.

**THE RULE THAT COMES OUT OF IT: register the suite in the SAME TURN you write it** — the same
shape as the standing rule that a build's `BUILD-LOG.md` row is written in the same breath as its
code. ⚠️ **The auction battery (`tests/run-all.mjs`) has NOT been checked for the same gap.**

## ✅ THE TWO RED SUITES ARE CLOSED — build 100, 25 Aug 2026. Kept for the LESSON, not the work.

**All three items are done and gated.** `build67`'s assertion was stale since [96] and is
repointed — stricter now, because it opens the section the way an admin does and proves the rows
are REACHABLE, not merely present. `build80`'s comment-stripper is fixed. The group rename no
longer calls `prompt()`; it is an inline row on the card. **Schedule battery is fully green for
the first time in weeks: 49 suites / 1,293 assertions, zero skipped.**

**THE LESSON THAT OUTLIVES THE FIX, and it is the largest of the day:**

> **A `/*` inside a LINE comment opened a block comment 347,322 characters long** — about 70% of
> `admin/index.html` — because the stripper removed block comments FIRST. Every textual assertion
> in `build80-test` had been running on the remaining 30%, so its `prompt()` gate never fired
> while a real blocking `prompt()` sat in the group rename.

**Both halves are now standing rules in `START-HERE.md` §3** (rule 10's second half, and rule 8's
third kind of lying gate). **The page still contains three `/*`-in-a-line-comment instances and
they were left alone deliberately** — they are valid JavaScript, and rewriting prose to appease a
regex is the exact "test the prose" error §3 rule 10 warns about. **The tooling was fixed instead.**
⚠️ **Any other suite that strips comments has the same hole until it is checked.** Not audited.

## 📒 THREE BUILD-LOG ROWS ARE MISSING — found 25 Aug, NOT written. Owner's call.

Found while checking whether shipped sections could leave this file. **Each one is why those
three sections STAYED** — the standing rule only lets work leave `TODO.md` once its record exists
elsewhere, and for these it does not.

1. **Schedule build 96** has no row in `schedule/BUILD-LOG.md` (rows run 98, 97, 95, 94, 93).
   It appears only inside the 97 row as an honesty baseline. The commit `16e5977` carries a full
   description, so a faithful row can be written from it — **but that is reconstructing history
   under a build freeze, so it needs his word first.**
2. **The CRNA stamper's 17 Aug "BUILT / HARDENED" work** has no row in either log — the auction
   log starts at build 270 (18 Aug). Later guard versions (v2, v3) DO have rows. The mandate for
   the tool lives inside `DECISIONS.md` §63 and §64, never as its own ruling.
3. **The 18 Aug overnight directive** that produced `schedule/design/UX-PLAN.md` has no row and no
   §. Later builds cite the plan as their source, so the artefact is load-bearing and its
   commissioning is unrecorded. It survives only in this file and `schedule/design/README.md`.

**Recommendation, not a decision:** write rows 1 and 3 from the commits and the artefact, leave 2
alone (the mandate is already ruled). Nothing done unasked.

## ✅ STAGE 5 IS IN BUILD — 105 · 106 · 107 FILED, 26 Aug 2026. §110/§111.

**Owner lifted his own hold: *"Go with stage 5 and continue as you can with any of the other
items that fold into it… as little prompting from me as possible."*** §110 records that, and
records that he relaxed the APPROVAL gate for one session and touched no QUALITY gate.

· ✅ **105 — the rules frame + Minimum rest.** `dailysched/rules`; types are code and instances
  are data (§11); §10's targets UNION (everyone · category · group · named people · any mix);
  three severities (§111 ①) that never veto (§4); the checker; the conflict report
  (who · when · which rule · what specifically); and a rule that cannot see anything SAYS SO,
  naming the count. **It REPORTS — `mutateCell`, `addCellShift` and auto-populate untouched.**
· ✅ **106 — the *Not more than* cap type (§111 ②, closing §21's deferral).** N of a TAG per
  Sunday-anchored week or calendar month. **It points at a tag, never at `kind`** — §27's Eye
  Call is named "call", runs daytime, and must not count. A week straddling the month boundary
  says so rather than under-counting.
· ✅ **107 — printable sheets.** One sheet per site, a day or a whole week, and a by-family
  shift selection stored as EXCLUSIONS so a shift added later is on the paper with no write.
  An unreadable month prints "not read", never an empty day.

**▶ THREE THINGS THAT ARE HIS CALL, NOT CLAUDE'S — none is a defect, all three are choices:**
1. **107 WIDENED HIS SPEC.** He said *"a weekly option for Richmond"*; the weekly option is
   offered for EVERY site, because sites are data (`siteList()`, §18) and a site name baked
   into a control breaks the day one is renamed. Superset of the spec. His to narrow.
2. **A weekly cap under-counts the month's first week** when the 1st is not a Sunday — the
   checker is handed one month. It says so on screen. Reading the previous month is a later fix.
3. **§109's "quotas as CONSTRAINTS" is still open.** Quotas still report only. Constraint
   solving across several quotas is the fuller Stage 5 work and has not been started.

**⚠ FB-3 INPUT: two new document listeners per admin page load** (`rules`, `printConfig`). §2
records 19–25 listeners per page and says the cost arithmetic gets redone before go-live. This
nudges it. Recorded, not acted on.

**A LESSON THAT COST THREE SUITES.** Build 107's section key was first `printshifts`, which
CONTAINS `shifts` — and five suites select section bars with `onclick*="shifts"`. Three catalog
suites clicked the Print page's bar and reported an empty shift catalog. **A section key that is
a superstring of another key is a trap for anything that matches loosely.** Key renamed to
`printpick`; all six selectors now name the exact `toggleSect('…')` call.

## ⛔ THE CALENDAR FEED CANNOT BE BUILT ON THIS INFRASTRUCTURE — found 26 Aug. HIS DECISION.

**§54a specifies a per-doctor SECRET-URL `.ics` SUBSCRIPTION with no sign-in** ("calendar apps
cannot log in"), contents being that doctor's shifts plus approved vacation. **A subscription
needs an HTTP endpoint that returns `text/calendar` and keeps returning a fresh answer.
GitHub Pages serves static files only.** So §54's "build it" cannot be executed as designed
without new infrastructure, and that is his call, not Claude's. The three real options:

1. **Firebase Cloud Functions** — an HTTPS function reads the token and returns the `.ics`.
   The only option that delivers what §54a describes.
   **⚠ CLAUDE WAS WRONG ABOUT THE COST AND CORRECTED IT THE SAME DAY (26 Aug), on the owner's
   challenge — *"firestore seems generally quite cheap"*, and he was right.** Checked against
   published pricing rather than recalled: **$0.03 per 100,000 document reads, with 50,000 reads
   free per day.** Even the naive shape — 35 doctors, hourly polling, ~120 day-documents read per
   poll — is ~100k reads/day, half of it inside the free tier, about **$0.50 a month**. Cost is
   NOT a reason to refuse this, and leading with it was a bad call.
   **THE TWO REAL COSTS, NEITHER OF WHICH IS MONEY:**
   · **A public endpoint with admin credentials on the project that runs the LIVE auction.** A
     function using the Admin SDK bypasses `firestore.rules` completely. Containable — see the
     recommendation below — but it is a genuinely new surface and §2/§92 make it his call.
   · **A deploy path he does not have.** Functions need the Firebase CLI and Node tooling; he
     pushes with GitHub Desktop and pastes rules into a console, and command-line developer
     tools are still not installed on his Mac (`xcode-select --install` is an open item).
2. **Static `.ics` files committed to the repo — REJECTED, and it should not be reconsidered.**
   `vacation-kp.github.io` and `schedule` are PUBLIC repos. This would publish named doctors'
   schedules at guessable-by-enumeration URLs, and they would be stale until the next push.
3. **A client-side "download my calendar" EXPORT** — buildable today, zero infrastructure, zero
   ongoing cost, no secret URL, no rules surface. **But it is a one-time file, not a
   subscription**, so it loses the thing IDEAS 1b called the whole point: *"updating
   automatically"*. Honest half-measure, not the feature.

**Nothing was built.** §0 rule 4 and §3 rule 1: this is ambiguous in a way the code cannot
settle, so it stopped and asked. **§54's design-and-build go does NOT authorise adding Cloud
Functions to the shared project** — that is a decision of a different kind.

**▶ CLAUDE'S RECOMMENDATION, given 26 Aug: *"i'm ok with some additional cost"*. NOT YET RULED.**

**Build it — but store the finished `.ics` TEXT in a per-doctor document, and let the function do
nothing but hand it over.** `dailysched/feeds/{token}` holds the rendered calendar; the endpoint
looks up the token and returns the string.

Why this shape rather than the obvious one:
· **The function never queries the schedule, so it cannot leak anything else.** It reads ONE
  document path and has no code that could range over `vacations/*` even by mistake. That turns
  the security objection from "trust the code" into "there is nothing there to get wrong" — which
  is the only version of this worth putting on the auction's project.
· **One read per poll instead of ~120**, which puts the whole feature inside the free tier
  permanently rather than merely cheaply.
· **~20 lines with no schedule logic.** When the data model moves — and it will, Stage 5 is not
  finished — the change lands on the WRITER inside the admin page, which is tested here, not on
  an endpoint deployed by other means.
· The writer costs one small write per affected doctor when a day changes. Writes are $0.09 per
  100,000, with 20,000 free per day.

**Build it NOW rather than at release, and keep §54's release gate SHUT.** While no doctor is
subscribed, a bug costs nothing; the week it is announced is the worst possible week to be
finding them. The token needs its own ruling: where it lives, and who may regenerate it.

## 🛑 WINDING DOWN THE SCHEDULE — owner, 26 Aug 2026. WHAT IS LEFT OF WHAT HE HAS RULED.

**He asked for exactly this list, so it is the answer and not a summary.** Checked in the code
on 26 Aug, not recalled.

**① STAGE 5 — THE RULES ENGINE. ▶ IN BUILD, and the hold is LIFTED (§110, 26 Aug).** Step 10 of
the approved sequence. The frame plus two rule types are filed as 105 and 106 — see the section
above. What is left of it is the fuller constraint work, not the frame. His plan for it is in this
file and need not be re-derived: one thin vertical slice, Minimum rest, end to end. Rulings that
already bind it — fairness is **"weighted burden by FTE"**, never raw counts; supervision
ratios/pairing; quotas become CONSTRAINTS here rather than readouts (§109).
**The severities question is ANSWERED — §111 ①: three, Serious / Warn / Note, and none of them
ever vetoes anything (§4).** `design/RULES.md`'s table is the spec and is built as written.

**② THE CALENDAR FEED — §54 and §54a. ⛔ BLOCKED ON INFRASTRUCTURE, NOT ON EFFORT.** Still not
built, and it cannot be built as designed on GitHub Pages: a subscription needs a live endpoint.
**The three options, their costs and the one that is rejected outright are in the section above.**
This is his decision now, not a queue item.

**③ PRINTABLE SHEETS — ✅ BUILT AS 107, 26 Aug.** His spec in full: a daily sheet per site, a
weekly option, and a print config saving a by-family selection with each shift individually
selectable, persisted. **One widening is flagged above and is his to narrow** — the weekly option
is offered for every site rather than Richmond alone, because sites are data.

**④ OVERNIGHT-CALL TRACKING ACROSS YEARS — ✅ BUILT AS 108, 26 Aug.** Checked first: by day is
the grid, and by month and by year were ALREADY the Stats page's two modes, so only *across
years* was built. A person × year table counting the **tag** (never `kind` — §27's Eye Call),
with sealed totals in `dailysched/callArchive` so a year costs one read instead of twelve. A year
nobody computed reads **·**, not 0; an incomplete or partly-unread year says so in the table.

**⑤ THE COLLAPSE SWEEP — ✅ CLOSED BY §115, 26 Aug.** He accepted all three exceptions and the
sweep is OVER; the CONTROL was styled instead (build 111). The three exceptions below stand, with
their reasons, and **§104 no longer acquires new ground** — a later build does not get to collapse
something new by citing it. Kept for the reasons, which are still the record:
§104 says *anything used occasionally is collapsed by default*; build 99 applied it everywhere
except **Reports** (its settings already sit in an `open <details>` — a bar there is a fold inside
a fold, and `renderReports` draws five targets in one function), **Simulator's Fill/Wipe**
(`renderRehearsal` assigns `className` outright, so a sweep's class is wiped, and a bad edit
leaves a lock that LOOKS unlocked), and **Stats** (it has no collapsible panel). Reasons are in
`schedule/BUILD-LOG.md`'s build-99 row.

**NOTHING ELSE HE HAS RULED ON IS OUTSTANDING.** Quotas (§108/§109) shipped as 102; the staffing
ranges (§102) shipped as 103 and 104; the word STAFFING (§103) as 101; collapse (§104) as 96–99;
the group fairness flag (§26/§43) as 93. The six catalog items and the four clutter items are all
closed or ruled.

## ▶ QUOTAS — NAMED AND UNBLOCKED (§108, 25 Aug). ONE QUESTION STILL OPEN. NOT STARTED.

**Owner: *"Let's go with quota and this will be its own separate set for times when a variety
shifts contribute to the same quota for a day."*** So: the word is **QUOTA**, and a quota is its
own named set of shifts, independent of `family`. **"Shift pool" is retired** — where older notes
say it, they mean this.

✅ **ANSWERED — §109: a shift MAY sit in two quotas.** Membership is many-to-many.
⚠️ **And that changes what a quota is:** two quotas over one shift on one day do not compete,
they both hold — so a quota is a CONSTRAINT, which is Stage 5's shape. **The first build must not
pretend to solve constraints it cannot.** Proposed, NOT ruled: quotas as READOUTS first (set,
total, and a met / short / could-take-more report per quota), with auto-populate still targeting
each shift's own MIN; true multi-constraint filling waits for Stage 5.

**Already ruled and unchanged (§102/§108):** auto-populate fills to **MIN** · **MAX is an
indicator, never a refusal** · **at-min-below-max is acceptable**, so coverage needs a third
reading (*covered · could take more*), not a binary. And the standing trap: the existing per-shift
`capacity` is already a maximum in all but name — **the range should ABSORB it**, not sit beside
it, or the build-78 defect shape returns (two fields meaning one thing).

✅ **BUILT AS ADMIN 102 (filed 25 Aug, awaiting push).** The page, `dailysched/quotas`, the
total as a stacking rule list, many-to-many membership, and the month readout. **It REPORTS
only** — `demandOn()` and auto-populate are untouched, and the suite pins that.

**▶ WHAT IS LEFT OF THE QUOTA IDEA, and it is the bigger half:**
· ✅ **[103a] SHIPPED (filed) — the SHAPE.** A rule is `{when,min,max?}`; the old `{when,need}`
  reads as min=max=need with **nothing rewritten**; `demandOn()` still returns the minimum so all
  seven consumers are unchanged; the editor takes an optional ceiling. `staffingOn()` exists and
  **nothing calls it yet**.
· ▶ **[103b] — the BEHAVIOUR.** Coverage's third reading (*covered · could take more*), the grid
  chip showing the range, and **`max` ABSORBING the per-shift `capacity`** so two fields stop
  meaning "the most this shift can take" (§108 ⑥).
  ⚠️ `capacity` is not only the auto-populate fallback — it is also live in the cell editor's
  "2/3 filled" label and in the swap-approval warning, which **already treats a maximum as an
  indicator rather than a refusal**. Both must be re-pointed with it.
· **Quotas as CONSTRAINTS rather than readouts** — auto-populate satisfying several quotas at
  once, with a conflict report and warn-and-override. **Stage 5 is no longer on hold (§110), and
  this is now the LARGEST single thing left in it.** The rules frame, two rule types and the
  conflict report all exist (105/106); what does not is anything that SOLVES rather than reports.

## ⭐ §106 — WHAT IS LEFT OF IT. Most of it was BUILT or KILLED on 25 Aug.

**Ruling: `DECISIONS.md` §106** (rewritten short the same day). Recorded here so the scoped
items that no longer exist are not rebuilt from a stale note.

**✅ DONE 25 Aug — the housekeeping pass itself.** `TODO.md` 1,662→1,222 · `START-HERE.md`
814→659 · `HANDOFF.md` 667→558. Nothing deleted; both blocks are in
`_archive/anesthesia/superseded-docs/`, dated. Surveyed by subagents, every boundary verified by
the session before cutting — one proposed range was rejected there because it would have severed
a live rule mid-sentence.

**❌ KILLED — the pre-build context GATE (was G1), and both threshold drafts.** There is no gate:
the owner judges rot, Claude reports the reading. Do not build `ctx.mjs`, do not invent a third
threshold. §106's final section records both withdrawn drafts and why.

**❌ KILLED — G3's "budgeted close" and G4's "housekeeping needs its own session".** Both were
premised on a context ceiling that does not exist where it was assumed: this session did the
re-grounding, six document edits, the whole archive pass and three subagent surveys in one go.

**✅ DONE 25 Aug — G2, the size discipline for `START-HERE.md`.** Owner: *"4 - go."* Its
tripwire is **700 lines** — lower than the other two files' 2,000 because it is the one file he
PASTES, so a line there is a tax on every session forever. Over it, a working rule not needed in
months moves to `START-HERE-ARCHIVE.md`. Filed in `HANDOFF.md`'s MAINTAINING section (rule 3a),
the §3 paperwork table, and `DECISIONS.md` §106 ①.

## ✅ SPACE-PADDING — CLOSED 31 Aug 2026: the owner ran the caret check — "there are no spaces in the e-mails." M-4 is now fully settled, both halves verified. (What follows is the record of what was checked and why.)

**M-4 itself is settled (§87): no capitals in any of the four documents, no data fix owed.**
Padding was never separately checked, is **invisible in the Firebase console**, and fails the
rules comparison exactly as a capital does — signs in, reads the board, **every bid write
refused**; in `adminAccess` it locks that admin out entirely.

**THE CHECK — do it on 27 Aug while the console is already open for the sign-in test.** In each
of `vacations/loginEmails`, `vacations/adminAccess`, `vacations/emailToUser` (**the KEYS**) and
`dailysched/adminAccess`: click a value into its edit box, press **End**, and look at where the
caret lands — past the last letter means a trailing space. Leading spaces show as a gap before
the first letter once the box has focus. **Any hit is fixed the M-4 way: clear it, then re-save.**

**THERE IS NO CODE FIX FOR THE STORED SIDE**, and this is settled, not open (§87's two limits):
rules cannot map over a stored list, and `emailToUser` is keyed by address so its keys can be
tested but never fetched. Trimming the INCOMING side in rules is possible and would be defence
in depth only — and it is inside `firestore.rules`, which **§92 closes without a specific
decision from the owner**. Do not open it for this.


## 📌 STANDING RULE — ARCHIVING IS CONTINUOUS, NOT A SPRING CLEAN (owner order, 25 Aug 2026)

> *"Can you build it into both handoff and todo to maintain the process of archiving old items?"*
> **The full rule lives in `HANDOFF.md`, in the section directly after the closing checklist.**
> Its `TODO.md` half is here because this is the file it acts on:
>
> **① SHIPPED WORK LEAVES THIS FILE AS IT ENTERS `BUILD-LOG.md`, IN THE SAME TURN.** Not later,
> not "at the next archive pass". A fact lives in ONE of the three files — **outstanding here** ·
> shipped in `BUILD-LOG.md` · settled in `DECISIONS.md` — and the moment something ships, this
> file is no longer its home. One deletion per build.
>
> **② THE TRIPWIRE: if this file passes 2,000 lines, the archive pass above is DUE**, and it is
> taken before the next feature build rather than "when there's time". Measure it, never estimate
> it: `wc -l TODO.md HANDOFF.md`. The trigger is a NUMBER for the same reason §62 made the commit
> cap a number — drift that depends on judgement is drift nobody notices.
>
> **③ `HANDOFF.md` archives a DATED ENTRY when it is NO LONGER NEEDED, never because it got old**
> (owner ruling §101, 25 Aug 2026, replacing the 14-day rule: *"i want to remove items no longer
> needed rather than remove by time."*). The test is one question — **if a fresh session never
> read this entry, would it do anything wrong?** Yes keeps it. And the habit that makes it work:
> **promote the lesson, then archive the story** — an entry you cannot archive because its lesson
> lives nowhere else means the lesson was never filed. Full rule: `HANDOFF.md`, the MAINTAINING
> section. Its permanent sections are never archived at all.
> **`DECISIONS.md` is EXEMPT and must not be trimmed** — a register should only ever grow, and a
> July ruling governs exactly as much as today's.
>
> **④ IF YOU CANNOT TELL whether an item is shipped, outstanding, parked or a standing rule, IT
> STAYS.** Uncertainty keeps it. Archiving something still owed is much worse than a long file.
>
> Step 2 of the closing checklist in `HANDOFF.md` is where ① gets enforced, every session.


## 🟢 SCHEDULE — THE QUEUE IS NOW BASE FEATURES (owner ruling §90, 24 Aug 2026)

> **"the key is getting it functional and build the base features."** The schedule has no users
> yet and has barely been tested by the owner, so the defect list is the wrong axis — the defects
> that matter cannot be known until the features exist and someone drives them.
>
> **IN ORDER — where the schedule actually stands, 24 Aug 2026:**
>
> · ⏸ **STAGE 5 IS PROPOSED AND ON HOLD — owner, 25 Aug: *"hold for now."*** Do not start it.
>   The plan he was given, kept here so it need not be re-derived: **one thin vertical slice
>   first** — the rules frame plus exactly ONE rule type, **Minimum rest (N hours between
>   shifts)**, end to end. That type needs no new data: it reads shift times from the catalog and
>   assignments from the day documents, both of which are now populated. It forces every
>   architectural decision into the open at once — a Rules page where instances are created and
>   pointed at everyone / a group / a subgroup / named people (§10's targets, which Stage 4 made
>   real), the checker, the conflict report (who · when · which rule · what specifically), and the
>   warn-and-override path with the override recorded. **Once that frame stands the other six
>   types in `design/RULES.md` are new predicates, not new architecture.**
>
>   **Two things settled before it resumes, so they are not rediscovered:**
>   · **`design/RULES.md` is sound but partly stale** — its own header says its status claims are
>     historical, and its "minimum slice" section proposes a data shape Stage 4 has overtaken
>     (`dailysched/people:{AR:{role:'MD'}}` never shipped; `categories` + `groups` +
>     `groupMembers` did, in §97's group→two-subgroups form). Re-point it, do not follow it.
>   · ✅ **The `fairness` flag IS on groups — this note was STALE.** Verified in the code 26 Aug:
>     `setGroupFairness` / `groupFairness` are live and build 93 shipped §43 (the pool moved onto
>     the group, with the per-person override). Nothing is owed here.


## 🔴 OWNER REVIEW OF THE SHIFT CATALOG — SIX ITEMS, 25 Aug 2026. NOT YET RULED ON.

> **Recorded the turn they were said.** The owner drove the catalog himself, which is exactly what
> §90 said would produce the findings that matter. **His words, verbatim, unedited:**
>
> 5. *"i need option to specify a range of staffing needs, basically 0-1 or 5-6 or whatever"*

## ▶ THE SCHEDULE QUEUE IS OPEN AGAIN — owner, 25 Aug 2026: *"we proceed with schedule builds."*

**Everything below is APPROVED AND SCOPED. None of it is a "go."** Read `START-HERE.md` §0: a
plan is presented and work waits for his explicit approval, per item. The order is lifted only by
the owner, in words, for the specific work.

### BUILD 99 — SCOPED, NEVER STARTED. Nothing was written to disk.

Two presentation-only changes that were to be grouped (his *"group together if reasonable"*),
chosen because they touch different code regions, so a failure stays traceable to one of them:

**(a) The collapse sweep, remaining pages.** Builds 96–98 collapsed the catalog, eligibility rows
and group cards. The sweep applies the same §104 test — *anything used occasionally is collapsed
by default* — to the rest of the admin, using the EXISTING `sectOpen` / `toggleSect` / `sectApply`
mechanism from build 96. **Never a second mechanism** (§3 rule 14). The judgement was made and is
recorded here so it is not re-derived from scratch:

| COLLAPSE (used occasionally) | STAYS OPEN (the reason you opened the page) |
|---|---|
| Users → *Admin Access*, *Add user* | Users → *All Users* |
| Open Shifts → *Post an open shift* | Open Shifts → *Posted* |
| Shift Families → *Add a family* | Families → *Families & shifts* |
| Simulator → *Fill*, *Wipe* | Simulator → *Rehearsal mode* (a safety state — never hidden) |
| Stats → *Which shifts count as overnight call?*, *Who counts toward the group comparison?* | Stats → *Fairness view* |
| Requests → *Decided* | Requests → *Pending* |
| Swaps → *Decided* | Swaps → *Awaiting…* |
| | Reports → steps 1 / 2 / 3 (a wizard; collapsing a step hides the path) |

**(b) The "Staffing" rename** (§103). A survey of `schedule/admin/index.html` at build 98 found
**107 occurrences of "demand": 88 identifier-like, 77 prose candidates.** §103's split is the
whole point and a future session must not "finish the job": **rename only what he SEES.**
`demandOn()`, `demandRules()`, `demandModal` and the stored `shifts.<id>.demand` field keep their
names. Renaming the stored field would be a data-model change wearing a cosmetic disguise.

⚠️ **Build 99 has no suite yet** — it was halted before one was written. Under §0 rule 5 the
verification is outlined before the build, not after.

---

## 🟠 THE COLLAPSE BRIEF — owner, 25 Aug 2026. APPROVED, DELIVERED AS BUILDS 96–98, REST HALTED.

> **His words, verbatim, in full:**
>
> *"shift eligibility needs to be cleaner, to many words visible and too crowded. each shift
> should start collpased and be opened by admin. it then stays open until closed intentionally or
> when page navigates to other section. nearly all sections should be collapsed by default, use
> some of your own judgement. anything that's used occasionally should be collpased by default.
> Add a shift and holidays for sure. I don't love how shifts ius organixzed. a scroller within a
> scroller can be hard to use. I think the entire shifts card should collapse by default and then
> expanded opens to a list of the families, which can then by further exoanded to find shifts.
> there also appears to be a double header at the top there that doesn't make sense."*
>
> **THE RULE HE GAVE, which is the part to build to:** *anything used occasionally is collapsed by
> default.* That is a test, not a list — apply it to every panel and be ready to say why each
> answer went the way it did. Named explicitly: **Add a shift** and **Holidays**.
>
> **THE OPEN/CLOSE BEHAVIOUR IS SPECIFIED AND MUST NOT BE IMPROVED ON:** a thing he opens **stays
> open until he closes it, or until he navigates to another section.** Not until re-render, not
> on a timer, not one-at-a-time-accordion unless he says so. Navigation is the only automatic
> close.
>
> **THE CATALOG GETS A THREE-LEVEL SHAPE, in his words:** the whole shifts card collapsed →
> expands to a list of FAMILIES → a family expands to its shifts. **And the reason matters more
> than the shape:** *"a scroller within a scroller can be hard to use."* So the fix is not a
> taller inner box; it is fewer things visible, so the inner scroller stops being needed.
>
> **TWO THINGS TO LOOK AT, NOT ASSUME:** the *"double header at the top there that doesn't make
> sense"* — find it and read it before touching it; and WHY the catalog's existing family bands
> (`catCollapsedFams`, already collapsible since S3) do not already solve this. **Do not add a
> second collapse mechanism beside one that exists** (§3 rule 14).
>
> ⚠️ **§93 APPLIES TO SHIFT ELIGIBILITY ESPECIALLY.** *"Needs to be cleaner"* is a symptom, not a
> design. That page has already been rebuilt once (§98 retired the 3,000-tick grid). Look at it,
> put a shape to him, then build.

## 🔴 OWNER REVIEW, SECOND BATCH — CLUTTER, COLLAPSING, AND THE WORD "DEMAND". 25 Aug 2026.

> **Recorded the turn they were said**, while he kept driving the site. **His words, verbatim:**
>
> *"The pages are generally feeling too cluttered and I think the answer is collapsable features.
> an example is the daily scheduling tab - I don't need to see a list of all MDs and CRNAs that
> will be assigned once and never looked at again. collapse that. same for each group, that
> shoud collpase unless i'm adding someone. If I have 10-20 groups 3eventually, the page would
> scroll forever and be cluttered. I don;t like using the word demand - present new options.
> maybe rules? maybe something else? The shift eligibility is difficyult to view - organise this
> better."*
>
> **FOUR ITEMS, AND THE FIRST THREE ARE ONE THEME.**
>
> **C1 · COLLAPSE BY DEFAULT, EVERYWHERE — this is §93 arriving as a general rule.** He is
> describing the same failure the Day Board was rejected for: a page that shows everything
> instead of the one thing he came for. *"Clean beats complete"* was a design rule for one
> screen; he is now asking for it as a property of the whole admin. **The load-bearing detail is
> his reason, not his instruction:** a list that is "assigned once and never looked at again"
> should not cost permanent screen space. That is the test to apply to every panel — **how often
> does he need to READ this, versus how often does he need to CHANGE it?** Read-often stays open;
> set-once-and-forget collapses.
>
> **⏫ RAISED AGAIN, A THIRD TIME, 25 Aug — AND THAT MAKES IT THE TOP SCHEDULE ITEM.** Verbatim:
> *"There;s too much data everywhere so it's hard to find what i'm looking for. Most things need
> to collapse throughout the site, at least for where i'll be entering data like rules and
> staffing and groups. this includes shift catalog especialy."*
>
> **He has now said this three separate times, in three different words, while trying to use the
> site.** That outranks the pool build (§102) on evidence, not on preference: **clutter is what is
> stopping him driving the schedule, and driving the schedule is what produces every other finding
> on this list** — which is §90's entire argument. Build the collapse work FIRST, then pools.
>
> **The named surfaces, in his order of pain:** the shift catalog *("especially")* · groups ·
> staffing rules · "most things" elsewhere. Note the catalog ALREADY has collapsible family bands
> (`catCollapsedFams`) — so the gap is not that collapsing is missing, it is that **the default is
> open and the unit is too coarse.** Do not build a second mechanism beside the one that exists;
> find out why the existing one does not solve it before adding anything (§3 rule 14 — check
> whether it already exists).

> **C1/C2 · ✅ SHIPPED AS ADMIN 96** (pushed, `16e5977`) — the catalog opens as four closed bars,
> Shifts opens to families, a family opens to its shifts, the inner scroller is gone and the two
> stacked column headers became one. **⚠ AND IT SHIPPED A REGRESSION the owner caught the same
> day:** an early return added for the collapsed Shifts section silently killed five unrelated
> renders on that page — the holidays list among them (*"The holidays section should list the
> holidays with remove feature as well"*). **Fixed in 97, with a six-assertion gate that runs the
> whole catalog page with Shifts closed.** The general lesson is in `schedule/BUILD-LOG.md`'s
> build-97 row: an early return added for one panel silently owns every line after it.

>


## The schedule's big builds — WHAT IS LEFT OF THE APPROVED SEQUENCE (owner, 18 Aug 2026)

All UX-PLAN §8 questions answered; sequence approved; each its own gated build. **Steps 1–9
(S1–S8 and Stage 4) have all SHIPPED** — their record is `schedule/BUILD-LOG.md`, and the entries
that stood here were archived on 25 Aug. **Only step 10 is left, and it is PARKED** (owner,
25 Aug: *"hold for now"* — the fuller plan is in §1 above). Everything under it is unbuilt.

10. **Stage 5 — the rules engine.** Fairness ruling (owner): **"weighted burden by FTE"** — never raw counts. Supervision ratios/pairing: NOT now (excluded with the OR board). Then the owner-picked set: **draft/publish · who's-on-now · open-shift claim alerts · in-grid violation badges · weighted fairness tallies · holiday multi-year equity.**

**OWNER SPEC verbatim — overnight-call tracking:** *"i want to track overnight calls by
person, by day, by month, by year, and across years"* — permanent year archives are a
stage-5/stats design input.

**OWNER SPEC — printable sheets ("brilliant"):** daily sheet per site; **weekly option
for Richmond**; print config **saves a by-family selection** with **each shift
selectable/unselectable in an easy-to-view method**, persisted.

**Calendar feed (§54)**: approved-to-build, release gate SHUT, owner slots it.

---

# 1a · NEW PROJECT — THE CRNA VACATION AUCTION (owner directive, 17 Aug 2026)

**Recorded the turn it was raised. Requirements in the owner's words:** *"The vacation site
is currently only being used by the MDs. The CRNAs are going to use their own vacation
auction that has the identical design- admin and user interfaces, pulls from the same code,
but has their own set of users, get their own alerts, has their own admin, with again
vacation goddess gmail as the dominant admin who cannot be kicked off. They will need their
own URLs that I want to be https://anesthesia-kp.github.io/vacation/crna and
[…]/vacation/crna/admin/. I want to update the landing page so that it says Choose a site
at the top. Department member websites will now have 3 options: Daily Scheduling at the
top, then MD Vacation Auction, then CRNA Vacation Auction. Admin Access only would have 3
as well: Daily Scheduling, MD Vacation Auction, then CRNA Vacation Auction. I assume we
will need 2 new repos and a new firebase. I want all the code to come from my main
vacation repo so that the 2 sites remain identical other than users."*

**Settled by the requirements themselves:** identical UIs · separate users, alerts, admins ·
dr.vacation.goddess@gmail.com is lead admin on BOTH, cannot be removed (the existing
LEAD_ADMIN mechanism) · URLs `/vacation/crna/` + `/vacation/crna/admin/` · landing page
"Choose a site" with 3+3 options ordered Daily Scheduling → MD Auction → CRNA Auction.

**DECIDED (17 Aug, owner):** labels — CRNA only for now (admin dashboard title
"Dashboard CRNA Vacation Auction — 2027"; staff page "CRNA" below the auction title,
above the Department line, same size; MD gets its label at the next regular MD build) ·
**shared EmailJS account for now, consider changing later** · one handoff/START-HERE/TODO —
this project adds no new doc files.

**CORRECTED ASSUMPTION:** zero new repos, not two. GitHub Pages can only serve
`/vacation/crna/` from inside the `vacation` repo — a new repo would get a different URL.
The CRNA site is a generated `crna/` folder in the existing repo.

**BUILT 17 Aug (working tree, gated):** `crna-stamp.mjs` — generates crna/index.html +
crna/admin/index.html + crna/versions.json from the MD pages with a fixed transform set
(Firebase config · seven shared-origin browser-storage keys renamed, or the two sites
would poison each other's caches on the shared anesthesia-kp.github.io origin · the
owner's labels · cross-links kept inside /crna/ · the Google-sign-in help note re-pointed
at the CRNA auth domain). LEAD_ADMIN stays dr.vacation.goddess@gmail.com, unremovable,
both sites. `tests/test-crna-stamp.mjs` (22 assertions, in the auction battery — now 15
suites) proves the crna/ files match a fresh stamp and goes RED on any hand edit or
un-restamped MD build. `crna/` is GITIGNORED until release — the release gate is removing
that line.

**Go-live default (owner did not pick; assumption stated 17 Aug): build now, PUSH BETWEEN
PHASES, landing page in the same push so its links are never dead.**

**HARDENED 17 Aug after the owner's "paramount that the sites never contaminate each
other":** the stamper now carries GENERIC guards, not just named ones — any storage key
that ever appears in the MD pages without a crna prefix in the stamped output, and any
absolute /vacation/ link that is not /vacation/crna/, ABORTS the stamp. Proven by planting
a rogue future key and watching it refuse. Residual risks accepted and named: shared code
is shared fate (a bug ships to both — the owner's own requirement); the shared EmailJS
quota (owner: reconsider later); the rules file must be published to both consoles (the
standing rule). Further separation (a different web origin) was considered and declined:
it would break the /vacation/crna/ URL and defends only against threats the guards already
close mechanically.

- [ ] **C8 · REVISIT WHEN THE SCHEDULING SITE GOES LIVE: scheduling links on the CRNA
      admin.** Owner ruling 18 Aug 2026: *"for now, they do not belong there"* — the Daily
      Schedule is an MD tool, so the stamper removes both Other Systems scheduling links
      from the CRNA admin, and the link guard aborts any stamp that would reintroduce a
      /schedule/ link. **When the scheduling site launches for real use** (and especially
      if a CRNA schedule ever exists), decide deliberately: restore the links (remove the
      stamper transform + guard clause together, then restamp) or keep the CRNA site
      schedule-free. Do not let a future MD-page edit resurrect them by accident — the
      guard makes that impossible without a deliberate stamper change.
- [ ] **C6 · Later, owner's call:** separate EmailJS account (decision recorded above);
      MD site label at next MD build; CRNA rows in the capacity report? (ask when B4 is
      designed — the two auctions have separate data, so B4 as specced is MD-only).

# 2 · VACATION AUCTION — live, in production

**State:** see the STATUS block at the top of this file. Rules PUBLISHED. **Queue empty as of
24 Aug 2026 — admin 304 / staff 164 / mobile 18 are live.** Battery, RUN on the device 24 Aug:
**54 suites / 2,050 assertions, zero skips.** (The "14 suites / ~1,075 assertions" that stood here
until today was 12 builds stale — counts come from running the battery, never from a note.)

## BULK-1 (per-week bulk approve/deny) — DECLINED BY THE OWNER, 24 Aug 2026

Claude proposed it after the owner noted the existing bulk actions are phase-wide only. Owner:
**"No, I don't want that."** **Do NOT re-propose.** Recorded the way RA-4's refuted findings are.
His "I want both options" means the EXISTING phase-wide bulk plus individual clicking made fast —
not a new per-week batch.

## Owner requests, 17 Aug 2026 — four items, verbatim

- [ ] **V6 · BATCH-ADD USERS — approved with enthusiasm 18 Aug, then DEFERRED same day**
      (owner: *"we can defer user list changes to later"*). Spec on record for when it is
      picked up: fields per line — initials, full name, gmail, FTE, kp e-mail; *"each
      user gets own line and order shouldn't matter since each variable cannot be
      confused with another. only name is a mandatory field to add."* Parser: kp.org
      e-mail → KP; other e-mail → Google login; number 0–1 → FTE; short all-caps token →
      initials; remaining words → full name; missing initials proposed in the PREVIEW,
      never silently. Create/update/rejected preview before ANY write (build-56 pattern);
      per-line duplicate-login refusal (225 guard); sanctioned save paths only. **Build
      ONCE on the MD auction admin** (shared roster serves the schedule; stamper serves
      CRNA). AUCTION BUILD: full battery + honesty, between phases.

## Watch items — dated facts, not defects

- [ ] **The `phases` document grows ~20KB per Phase-4 round against Firestore's 1MB cap.**
      Late 2027 on current rates. Detail: `HANDOFF.md` D3.

## Open defects on the live site — ONE home, and this is it

> ⚠️ **READING BUILD NUMBERS IN THIS SECTION.** A build number here is PUSHED AND LIVE if it is
> at or below the current live builds in the STATUS block at the top of this file (auction
> admin/staff/mobile, schedule admin/staff). Entries below were once written as "awaiting
> push" and stayed that way long after the push, which reads as unpushed work that does not
> exist — corrected 22 Aug 2026. **Do not write "awaiting push" into a permanent entry;**
> the queue at §1 is where in-flight work belongs, and `node status.mjs` is what tracks it.

*Consolidated from the 25 Jul code review; statuses re-verified 16–17 Aug. The full fix
sketches survive in `tests/docs/CODE-REVIEW-FINDINGS.md` and the archived copy of
OPEN-RESIDUALS.*

> ⚠️ **ID COLLISION.** `HANDOFF.md` Part B also uses the labels M1/L2/L3 for the **31 Jul
> Batch-D** items — different, closed defects. The IDs below are the **25 Jul code-review**
> numbering. Never quote a bare ID; name the list.

| id | what | status | neutralised by |
|---|---|---|---|
| **M1** | results e-mails can double-send if two admins press Send inside a slow >2-min window | **ACCEPTED for launch, 18 Aug 2026** (owner order: finish the vacation builds; owner July ruling stood: *"better to leave it alone than create new problems"*) | **one admin runs the auction** — complete. Unverified: 269's server-truth ledger reads may narrow it; do not assume |
| **L2** | mail-queue claim is claim/settle/re-check, not a transaction | **ACCEPTED for launch, 18 Aug 2026**; worst case one duplicate alert from a lagging background tab | nothing needed |
| **L3** | a bid queued on an offline device can replay into a freshly reset auction, stale phase tag, no error | **ACCEPTED for launch, 18 Aug 2026** — the code fix touches the launch lifecycle | **after Reset: Global Lock ON until Begin Phase 1**; glance at the board before going live |
| **L4** | an earlier phase's unsent results can become unsendable | **ACCEPTED for launch, 18 Aug 2026** — narrowed in 245 (auto-targets an earlier unsent phase) | send or rehearsal-skip each phase's results before beginning the next |
| **FB-1** | **Firestore daily read quota — RESOLVED by moving to pay-as-you-go (owner, 19 Aug 2026; DECISIONS §61). No code change.** Owner question, 19 Aug 2026. Modelled from the code, NOT measured: every staff client holds ~20 live listeners, and a listener costs one document read per change delivered — so ONE bid, which writes ~4 documents every client listens to (bids · changes · bidTimes · timer), costs roughly **4 × (connected clients)** reads. At ~60 doctors that is ~240 reads per bid action; a few hundred bid actions in a busy day crosses the free tier's 50,000 reads/day. **CORRECTION, same session:** Claude first said the log is "never trimmed". That was WRONG — `completePhase` archives the log and then writes `{log:[],resetAt:…}`, and Reset Auction does the same, so growth is bounded to ONE PHASE, not the whole auction. What remains true: within a phase the log is append-only, and the WHOLE document is re-sent to every listener on every append, so EGRESS grows with log size × clients × changes | **WATCH ITEM, not a defect — 19 Aug 2026. MEASURED: the live rehearsal peaked at 15,000 reads/day against the free tier's 50,000 — roughly 3.3× headroom, with the real group on the real site.** That supersedes Claude's code-derived estimate, which put a deadline day near the cap; the model was pessimistic about how many clients sit connected at once. **OWNER'S NUMBERS, 19 Aug 2026: 35 participants, and "roughly 400 bid actions on a busy
day."** Multiplier verified against the code rather than estimated — one staff bid writes
**3 documents always** (`schedule` · `changes` · `bidTimes`) **plus up to 2 conditionally**
(`timer` when the reset extends the deadline, `bestBids` when the bid crosses the previous
floor). All five carry a listener on EVERY staff page, so one bid action costs
`3–5 × (clients connected)` reads. At 400 actions:

| connected at once | 4 docs avg | 5 docs |
|---|---|---|
| 25 | 40,000 | 50,000 |
| 30 | 48,000 | 60,000 |
| 35 (all) | 56,000 | 70,000 |

plus ~3,000 for page loads. **The realistic band is ~43,000–63,000 against a hard 50,000
cap — straddling it, and the worst day is the phase deadline, exactly when an outage does
most damage.** On the free plan exceeding it DENIES reads until UTC midnight: the board stops
loading and nobody can bid, mid-phase. Treat as a LAUNCH BLOCKER while the project stays on
the free plan.

**THE FIX IS THE PLAN, NOT THE CODE.** Pay-as-you-go keeps the same free allowances and only
charges beyond them; going ~15,000 reads over on a handful of days is on the order of CENTS
per month. It converts a mid-phase outage into a rounding error. Set a low budget alert.
No code risk, no build, no suite — available tonight.

**Code levers become optional optimisation once the plan is changed**, not load-bearing: the
best of them (drop the staff `changes` listener, below) cuts ~25%, taking a 56k day to ~42k.
Worth doing as an audit item; NOT worth patching a live pre-launch auction for.

**Earlier estimates in this entry assumed ~60 participants and are superseded.** Every earlier
estimate in this entry assumed ~60 and is therefore too pessimistic — re-modelled at 35, a
heavy deadline day lands around 20k–36k reads against the 50,000 cap, i.e. **under, with real
but not generous margin**; only an extreme day (≈400 bid actions with nearly all 35 connected
at once) approaches it. **Not all users participated in the rehearsal either**, so the
measured 15k still needs scaling —  and the scaling is not linear. Reads = (write events) ×
(connected listeners), and BOTH grow with head-count, so they scale with roughly the SQUARE:
`reads_real ≈ 15,000 × (35 / rehearsal_participants)²` — rehearsal of 30 → ~20k · 25 → ~29k ·
20 → ~46k (thin) · 15 → ~82k (over). **The rehearsal head-count is the one number still
missing.** At 35 real participants the honest reading is: probably fine, worth one look at the
Usage graph after Phase 1 of the real run, and NOT worth an architecture change beforehand. Two caveats kept deliberately: the rehearsal **SKIPPED Phase 4**, so the P4 round lifecycle has never been measured (each round is a mini-phase; per-round activity should be smaller, as fewer weeks remain — but it is unmeasured, not proven), and a real auction may churn more than a rehearsal because the stakes are real. Re-check the console Usage graph after Phase 1 of the real run — that is the number that matters. Original analysis, kept for the reasoning: |  **OPEN — analysis only, 19 Aug 2026. Nothing measured, nothing built, no "go". OWNER CONFIRMED 19 Aug: the project is on the FREE (Spark) plan**, so both caps are HARD DENIALS, not overage charges — exceeding reads means the board and bidding stop working for the rest of the UTC day. Real usage and real document sizes remain owner-only facts. **The live rehearsal already generated real traffic — the console's Usage graphs for those days are the ground truth and settle this in minutes** | nothing yet. Two independent questions: (a) **the plan — now OPTIONAL insurance, not a recommendation.** At 15k/day the free allowances are never exceeded, and pay-as-you-go keeps those same free allowances before charging — so the bill at this usage would be zero either way. What it buys is the removal of the CLIFF: on the free plan an unexpected spike stops the auction until UTC midnight; on pay-as-you-go with a budget alert the same spike costs cents. Verify the current pricing in the console rather than taking this from here. Owner's call, no urgency; (b) **code levers, if wanted anyway** — note the two are NOT interchangeable: capping the STORED log cuts EGRESS only, and limiting what is DISPLAYED cuts nothing at all, because billing happens when the document reaches the client. The only lever that cuts READ COUNT is writing fewer listened documents per bid (today ~4: bids · changes · bidTimes · timer) or holding fewer live listeners. **Ranked options, none built, none costed against real numbers yet:** (i) **drop or defer the staff page's `changes` listener** — it powers only the Popcornometer and the "changes in last 24h" chip, both decorative, yet it costs a full 25% of every bid's read bill; the WRITE stays, so Fair Play and the admin log lose nothing. Cleanest win per unit of risk. (ii) **Unsubscribe listeners while the tab is hidden** (`visibilitychange`), resubscribing on focus — people leave this open all day and a backgrounded tab bills exactly like a watched one; potentially the largest single saving, but needs care so a re-focused tab cannot show a stale timer. (iii) **Fold `bidTimes` into the bids write** so one bid touches one document instead of two — another ~25%. (i)+(iii) together halve per-bid reads; adding (ii) could plausibly reach 70-80%. **All three are architecture changes to a live pre-launch auction and belong to the audit or a deliberately gated build, never to a rushed patch.** Distinct from the already-recorded `phases` doc growth (~20KB/round vs the 1MB PER-DOCUMENT cap) |

Cosmetic, accepted: the Approvals week-header clips at phone width. The dashboard loading
placeholder on a cold load is intended behaviour.

A residual leaves this table only when a build closes it and a suite proves it. **Eight rows left
it on 25 Aug 2026 under exactly that rule** — M3, L1, L5, TR-1, TR-2, TR-3, CL-1 and CL-2, all
closed by builds 270, 276 and 277 and each pinned by a suite. They are in
`_archive/anesthesia/superseded-docs/TODO-archived-2026-08-25.md`. **What remains here is what is
NOT closed:** four items formally ACCEPTED for launch (M1, L2, L3, L4) and one watch item (FB-1).

### FB-3 · The schedule site shares the auction's Firestore — and now its BILL — 19 Aug 2026

Owner question. **Verified in the code, not assumed:** both sites use the SAME Firebase
project, `vacation-25e8e`. The schedule keeps its own data under `dailysched/*` (adminAccess,
auditLog, callBaseline, callTotals, compPool, eligibility, fteMap, openShifts, requests,
sched_*, shiftCatalog, swaps, tags) and additionally READS `vacations/*` for the shared roster
and phase state. So **one free quota, one bill, covering both sites.** By contrast the CRNA
auction is a SEPARATE project (`crna-vacation`) with its own free allowance entirely.

**No impact today** — the schedule is a prototype with effectively no users, so it contributes
nothing to the numbers in FB-1.

**Real impact later, and it is the larger of the two.** The schedule pages carry **19 listeners
(staff) and 25 (admin)**, comparable to the auction — but the schedule is for the WHOLE
department, EVERY working day, all year, whereas the auction runs in bursts a few weeks a year.
When the schedule goes live it will plausibly become the dominant consumer of the shared quota.
**Re-run the FB-1 arithmetic for the schedule BEFORE it goes live, not after** — and note the
pay-as-you-go switch (DECISIONS §61) means the consequence is a bill rather than an outage,
which is the right way round but is not the same as free.

**Also worth knowing:** the existing `tests/sched/isolation-test.mjs` guard protects the shared
roster from unsanctioned WRITES. It says nothing about quota or cost — a runaway read loop on
either site now spends the other's money. Different failure, no guard.

### FB-2 · Reads tracker — **DECLINED by the owner, 19 Aug 2026 ("skip the tracker"). Kept only as a record — do not re-propose.**

**What is NOT possible.** The Firestore client SDK exposes no read counter. The authoritative
daily figure lives in Google's billing system, reachable only via Cloud Monitoring with server
credentials — which a static public page cannot hold. So an admin card showing the TRUE
"reads used today" cannot be built. Anything shipped here is an estimate and must be labelled
as one on screen.

**What IS cheaply possible.** Both pages already funnel every listener through a single
wrapper — `function onSnapshot(ref, next, error)` (staff ~line 770, admin ~line 1683). That is
a clean choke point: counting each delivered snapshot there gives an EXACT read count **for
that tab**, at zero Firestore cost. Two useful readouts follow:
1. **This tab, exact** — reads since load, and a per-minute rate.
2. **Fleet estimate** — derived from data the admin page already holds: today's change-log
   entry count (a good proxy for bid actions) × the verified 3–5 listened-documents-per-action
   multiplier × an assumed concurrency, with all three assumptions shown so the number is
   auditable rather than magic. Costs no extra reads or writes.

**Anti-pattern to refuse if it comes up:** a shared counter document that every client
increments per read. That adds a WRITE per read AND re-broadcasts the counter to every
listener, making the exact problem worse. Do not build it.

**Priority, stated plainly:** a tracker WARNS, it does not PREVENT. The plan change (FB-1)
prevents. The tracker's real value is if the project STAYS on the free plan — the console's
usage figures lag, so an in-page live estimate would flag a hot day before the wall is hit.
On pay-as-you-go most of that value disappears. Also note this is a NEW FEATURE on the live
admin page during an audit hold: it needs its own "go", its own suite and its own honesty
check like any other build.

### FB-4 · Abuse/billing hardening for pay-as-you-go — RAISED 19 Aug 2026 (owner asked)

Owner question, 19 Aug: *"is there any risk to a hacker stealing my api keys and running up a
bill?"* The premise needed correcting: **the browser API key is not a secret and never was** —
it ships in the public page source of every Firebase web app by design. The real question is
what an unauthenticated stranger can already do with it.

**WRITES: well defended.** A write needs a verified Google account listed in the admin-managed
login allow-list; bid writes are confined to the caller's own keys, server-clock gated and
extend-only. Verified against the rules file 19 Aug. No change proposed.

**READS: this is the exposure.** The `vacations` read rule ends in a public default, and the
schedule collection (including its subcollections) is public too. Metered reads with no hard
cap on Blaze is the billing surface. **Order of magnitude, so it is not over-feared:** past the
50k/day free allowance it is roughly 3-6 cents per 100,000 reads, so a sustained deliberate
scrape is tens to a few hundred dollars a day — a bad week, not a ruinous one. A runaway
listener loop in OUR OWN code is the likelier cause than an attacker (see FB-1/FB-3).

**AGREED NOW — owner-owned console work, no code, no deploy:**
- [x] **DONE 19 Aug 2026 — owner moved to Blaze and set billing alerts.** An alert notifies,
      it does not stop spending (DECISIONS §61); it was set low on purpose so a bad day
      surfaces in hours rather than at month end.
- [ ] HTTP-referrer + API restrictions on the browser key in the Google Cloud console.
      **Caveat to test immediately after:** the Google sign-in handler is served from the
      project's own `firebaseapp.com` auth domain, so that domain must be allowed too or
      sign-in breaks. Change it, then load every live page and sign in once; be ready to undo.
      The CRNA project has its OWN key and needs the same treatment separately.



### ENFORCEMENT — the checklist, kept for the record and for the next project

**Current state: MONITORING. Pages carry App Check tokens; NOTHING is rejected.** That is a
safe resting state and there is no deadline on leaving it. Enforcement is console-only — no
code, no deploy, no push — and it is the ONE action in this whole thread that can lock real
people out, which is why it is the owner's to take deliberately.

**⚠️ FIRST READING OF THE REAL NUMBERS — 19 Aug 2026, owner screenshot. DO NOT PANIC AT THE
98%.** The console showed, over a **7-day window (Aug 12–20)**: verified **2%** (5.4K), unverified
**"outdated client" 98%** (314K), unknown-origin **0**, invalid **0**.

**What that actually means, and why it is fine:** App Check only shipped on 19 Aug. Every request
in that window from BEFORE the deploy came from pages with no App Check SDK in them, which is
exactly what "outdated client" counts. So the 98% is almost entirely HISTORY, not a live problem.
The chart bears this out — orange sits at 100% until 19 Aug, then falls away as blue (verified)
climbs to ~100% at the right-hand edge. **The 7-day aggregate is the wrong number to read.
Switch the date range to 1 day and look at the current day only.** Also reassuring: unknown-origin
and invalid are both flat ZERO — nobody is hitting the project from anywhere unexpected.

**🔑 THE REAL PRE-ENFORCEMENT STEP, and the site already has the tool for it.** "Outdated client"
also covers **a real person sitting on a CACHED copy of the old page**. Enforce while anyone is
still on pre-App-Check bytes and that person is locked out with no error they can act on. The fix
is already built: `adminSettings.requiredBuilds` — the admin page arms it as a one-way ratchet,
and every client running an older build gets `_rbShowGate`, a full-screen "A new version of this
page has been published — reloading in 5…" overlay that force-reloads with a cache-busting `?v=`.
**So the order is: arm requiredBuilds to the current builds → give it a day so open tabs and
cached clients roll over → confirm the ONE-DAY verified figure is ~100% → only then Enforce.**
That converts the main lockout risk into a five-second reload the user barely notices.

**Also worth a look while in there — a possible FB-1/FB-3 signal.** 320K App Check requests over
7 days is ~45K/day. The rehearsal measured ~15K Firestore *reads*/day. These are NOT the same
unit (an App Check request is not a billed document read), so this is not a contradiction — but
the gap is big enough to be worth checking the actual Firestore usage graph now that the project
is on Blaze and the consequence is a bill rather than a cliff.

**✅ SECOND READING — LAST 60 MINUTES: 100% VERIFIED, 870/870.** Outdated 0 · unknown origin 0 ·
invalid 0. This settles the previous entry: the 98% was history, and the live mechanism works —
tokens are being issued and accepted on the real site.

**⚠️ CLAUDE CHANGED ITS RECOMMENDATION HERE. Recorded, not quietly shifted.** Through the day
Claude said "wait several days before enforcing." That was right when the only evidence was a
7-day aggregate dominated by pre-deploy traffic. With a clean 100% window it is worth stating
the counter-argument honestly, because it is stronger:

- **The blast radius will never be smaller than it is right now.** No auction is running, no bids
  exist to lose, go-live is weeks away, and enforcement is a ONE-CLICK revert. A problem found
  today costs an apology; the same problem found during Phase 1 costs the auction.
- **Waiting does not, by itself, generate evidence.** The 870 requests in that window are
  essentially the owner plus Claude's browser probes. Sitting in monitoring for another week
  while nobody logs in produces exactly the same knowledge — and then enforcement gets flipped
  closer to launch, which is strictly worse.

**✅ CRNA PROJECT READING — 100% verified, 211/211, every failure row ZERO.** All of it on
19 Aug, because that site has no history to speak of. Two things this proves:
1. **The `crna-stamp.mjs` key swap works END TO END, live.** The CRNA pages carry a DIFFERENT
   reCAPTCHA key from the MD site and it attests against the *CRNA* Firebase project. That was
   proven statically by the stamper canary; this is the running confirmation.
2. The CRNA project has essentially no users — the 211 requests are the owner's own testing.

**⚠️ THAT REVERSES THE PROJECT ORDER CLAUDE GAVE EARLIER.** The first version of this checklist
said "enforce `vacation-25e8e` first, `crna-vacation` separately and later," reasoning that the
live site should go first. **That was backwards.** `crna-vacation` has NO users and NO live
auction, so enforcing there costs nothing if it goes wrong and is a free rehearsal of the switch
itself — you learn what the Enforce button does, and what a blocked request looks like, on a
project where nobody is harmed. **Do CRNA FIRST.** Be clear about what it does and does not
prove: it tests the MECHANISM, not browser diversity, because there are no real users on it.

**⚠️ THE SCHEDULE SITE HAS NO BUILD GATE — checked 19 Aug, and it matters here.**
`requiredBuilds` / `_rbShowGate` exist ONLY on the auction pages; `grep` finds zero references on
either schedule page. But **the schedule shares the `vacation-25e8e` project**, so enforcing App
Check there enforces it for the schedule too — and a cached schedule tab has nothing to force it
to reload. Low stakes today (the schedule is a prototype with essentially one user), but the fix
is trivial and manual: **after enforcing, hard-refresh both schedule pages.** Worth a real build
gate before the schedule goes live for the department — logged against FB-3.

**REVISED RECOMMENDATION — enforce soon, but only in this order:**
0. **✅ ARMED 19 Aug 2026 — verified live: `requiredBuilds` = {index:150, admin:284}, matching
   `versions.json` exactly.** Read straight off `vacations/adminSettings` (publicly readable) after
   the owner opened the admin page. Note for a fresh session: **there is no button.** The ratchet
   fires automatically on admin page LOAD — the page's own `versions.json` fetch sets
   `window.__vlatest` and pokes `_rbTryRatchet()`. Before he opened it, the stored value was
   {148, 283}: proof the mechanism has been working all along, just two builds behind.
   **And the protection is effectively IMMEDIATE, not a day's wait:** the gate fires off the
   `adminSettings` snapshot every open tab already holds, so an open stale tab overlays and
   reloads within seconds; a closed-but-cached client gets it on its next load, then reloads to
   a cache-busted `?v=150`.
1. **Arm `requiredBuilds` FIRST.** Cached clients are the one real lockout path
   ("outdated client" covers a person sitting on stale bytes). `_rbShowGate` force-reloads them.
   This step is not optional and it is what makes the rest safe.
2. **Enforce `crna-vacation` first** (free rehearsal, no users), THEN **Cloud Firestore on
   `vacation-25e8e`**. Never Firebase Auth, on either project.
3. **THEN MANUFACTURE THE EVIDENCE — this is the step that actually matters and it is the owner's
   to do.** Ask a handful of real participants to sign in this week, deliberately including
   phones, a non-Chrome browser, and anyone known to run ad-blockers or a VPN. **Enforcement only
   teaches you something if people actually use the site.** Enforcing and then leaving it idle
   until launch converts a test into a gamble.
4. Watch for "invalid" or "unknown origin" appearing above zero, and for anyone reporting a page
   that will not load. Revert on the first real report, diagnose second.

**📖 HOW TO READ THAT SCREEN — owner asked 19 Aug; definitions are Google's own, not paraphrase.**
**App Check has NOTHING to do with who is signed in.** It asks "did this request come from our
real app?", never "who is this person?". A visitor who has never signed in still produces
**verified** requests the moment they load the page.

| category | Google's definition | what it would mean here |
|---|---|---|
| **Verified** | has a valid App Check token | normal — the only category allowed once enforcing |
| **Outdated client** | missing a token; "might be from an older version of your app before you added the App Check SDK" | someone on cached/stale bytes — the `requiredBuilds` gate exists to fix exactly this |
| **Unknown origin** | missing a token AND "don't look like they come from the Firebase SDK… might be using stolen API keys, or forged requests" | **the row that actually matters** — somebody scripting against the project |
| **Invalid** | has an INVALID token; "an inauthentic client attempting to impersonate your app, or emulated environments" | impersonation attempt, or an odd/emulated browser |

**A ROSTER TYPO DOES NOT APPEAR HERE — different layer entirely.** If a login e-mail is entered
wrong, that person still signs in to Google fine and still produces VERIFIED App Check requests;
they simply are not recognised as a participant and cannot bid. **That failure surfaces on the
admin Users page via the `signInMisses` log** (the staff page writes the attempted address there
on a failed match), which is where to look — not App Check. Wrong-address lockout is already on
the launch checklist for this reason.

**The one case where a REAL person shows as unverified** is reCAPTCHA scoring their browser as
bot-like, so no token is ever issued. Claude has NOT confirmed which bucket that lands in
(most likely "outdated client", since the request arrives tokenless from a Firebase SDK) — treat
the bucket as unconfirmed. **The reliable signal is a person telling you the site will not load.**

**Before flipping anything — read the numbers, do not guess:**
1. Firebase console → the project → **App Check → APIs tab → Cloud Firestore**. It shows
   requests split into **verified / unverified / outdated client**. Let a few ordinary days
   accumulate first, ideally days on which several people actually used the site.
2. **What you want to see: verified ≈ 100%, unverified ≈ 0.** A steady trickle of unverified
   is NOT necessarily an attacker — it can be one real person whose browser is scoring badly
   (heavy ad-blocker, unusual VPN, very locked-down browser). **reCAPTCHA v3 gives such a
   person no puzzle and no way to prove themselves; they would simply stop being able to use
   the site, silently.** That is the whole reason for watching first.
3. If unverified is not ~0, STOP and bring the numbers back rather than enforcing. Options
   then include raising the App Check risk threshold — do not just push through it.

**Flipping it, when the numbers are clean:**
4. Enforce **ONE service at a time**, starting with **Cloud Firestore**, and **one project at
   a time** — `vacation-25e8e` first (auction + schedule), `crna-vacation` separately and later.
   Never both projects in one sitting.
5. **Do it BETWEEN phases, never mid-phase**, and never on a bidding deadline day. Standard
   auction deploy discipline applies even though no code ships.
6. Immediately after: load every live page and sign in once — auction staff, auction admin,
   schedule staff, schedule admin. Then have one OTHER person do the same, on their own device.
   Claude's machine passing proves very little about 35 other browsers.
7. **The revert is one click, in the same console screen.** If anything looks wrong, switch
   enforcement off first and diagnose second — there is no prize for leaving it on.

**Do NOT enforce App Check on Firebase Auth.** Only Firestore is in scope here. Auth enforcement
would put App Check in front of the sign-in path itself, which is exactly the blast radius §1
exists to avoid, and it buys nothing against the read-metering exposure FB-4 is about.

**Non-negotiable sequencing:** register the sites and run in MONITORING mode first — monitoring
blocks NOTHING and simply reports how much traffic would have been rejected. Only enforce once
the console shows the real pages producing valid traffic and no unexplained rejections. Enforce
per-service, and keep the ability to switch enforcement back off in one console click.

### FB-5 · Read-privacy — the documents that are public and need not be — RAISED 19 Aug 2026

Owner, 19 Aug: *"i want both billing security and privacy."* Distinguish two threats, because
they have different fixes:
- **Outsider** (no account): fixed by App Check (FB-4), not by rules.
- **Participant** (a real bidder, signed in, using devtools): fixed ONLY by rules.

**The finding, shape only — this repo is PUBLIC.** Several documents that carry live auction
state read publicly, with no sign-in required. **The site's own code does not depend on that:**
the full bid listener is attached on the SIGNED-IN path only, and the pre-sign-in screen waits
on a small, identifiable set of other documents. So a tightening exists that the login
bootstrap would not notice. Verified by reading both pages' listener attachment points, 19 Aug.

**THE QUESTION IS ANSWERED — BY THE CODE, not by a ruling. Verified 19 Aug 2026.** Claude was
about to put "open or sealed auction?" to the owner. It did not need asking: the staff board's
week renderer already collects every OTHER participant's bid for each live, unlocked week and
paints it under a "competitors" label. **This is an OPEN auction by design and always has
been.** Consequence, and it is the good kind: gating these reads takes NOTHING away from any
participant — they already see this in the UI — so the change is functionally invisible to the
35 bidders. **The entire privacy gain is against NON-participants:** strangers, and colleagues
who are not in the auction, who today can read the roster, the live bids and the whole daily
schedule without ever signing in.

**It is TWO changes, not one — do not under-scope it.** A rules edit alone is not enough,
because several of the listeners in question attach at page load, BEFORE sign-in. Gate the
document without moving the listener and the page acquires a dead permission-denied listener
at boot — the exact failure the rules file's own comments warn about in three places. **The
client must move those listeners onto the signed-in path first**, following the pattern the
staff page ALREADY uses for its sensitive documents (an unsub list attached only after
sign-in). That precedent is the reason this is a medium build and not a research project.

**Sequencing, and the risk direction that sets it.** Gating a read the bootstrap silently needs
does not degrade the site — it LOCKS EVERYONE OUT. So: client change ships and is proven first;
rules follow; each candidate document is proven unnecessary before it is gated, ONE AT A TIME,
never as a batch. Gates as normal: isolation test, both batteries, honesty check from an
explicit SHA, and the console publish lands BEFORE any dependent client push (§2).

### FB-5 · EXECUTION PLAN — analysed 19 Aug 2026, evidence in hand, NOT yet built

**Owner said GO. Claude did the ANALYSIS and deliberately stopped before the code**, because
FB-5 is the one change here that can lock all 35 people out, and it was the tail of a very long
session (START-HERE §4: volunteer it, do not quietly compensate). Everything below is read from
the pushed pages, so a fresh session can execute without re-deriving it — but **re-verify each
claim against the current bytes before acting; that is the whole point of the exercise.**

**METHOD USED, so it can be repeated for the admin pages:** find every function that runs on the
LOGIN SCREEN before sign-in resolves, and read what data each touches. Those are
`initSigninScreen` (via `refreshLoginUsers`), `updateSigninPhase`, `biddingOpen`, `_rbGateCheck`,
`_acCheckRemote`, `renderWhitelistBanner`, plus the `signinReady()` readiness flags.

**STAFF AUCTION PAGE — the login screen GENUINELY needs these five. Do NOT gate them:**

| doc | why the login screen needs it |
|---|---|
| `phases` | `updateSigninPhase` + `biddingOpen()` print "Phase N · Bidding open" |
| `adminSettings` | `_rbGateCheck` (required-build gate) and `_acCheckRemote` (config reload) |
| `usernames` | `initSigninScreen` gates on `_usernamesReady` and calls `displayName()` |
| `userList` | `initSigninScreen` tests `names.includes(remembered)` and `names.length` |
| `passcodes` | only via the `_passcodesReady` flag in `signinReady()` — **the passcode feature is RETIRED (`updateLoginPwState()` is an empty stub), so this listener is probably deletable outright. A tidy win, but it is its own change with its own "go".** |

**Candidates that can be gated — the login screen does not touch them.** Each still needs its
listener moved onto the signed-in path first, because they attach at module load today:
`locks` · `changes` · `bestBids` · `bidLowerings` · `bidTimes` · `whitelistConfirm` · `fteMap` ·
`bidPhase` · `slots` · `timer` · `welcomeLog` · `mailStats`.
Two notes: `timer`'s listener calls `startCountdownTick()` at module level, so moving it needs
care; `whitelistConfirm` is safe because `renderWhitelistBanner` returns early until
`selectedSignInName` is set, which only happens after a real sign-in.


### FB-5 STAGE 2 — the map, analysed 19 Aug 2026. NOT built. No "go" yet.

**Done as read-only prep while the owner was away.** Every claim below was read out of the
pushed pages; re-verify before acting, but the derivation should not need repeating.

**⚠️ THE ASYMMETRY THAT DECIDES ALL THE RISK — verified, and it is not intuitive.**
The two page families behave completely differently when a read is denied:
- **ADMIN pages retry.** Their `onSnapshot` wrapper catches the error, flags a dead feed,
  shows a banner, and **re-attaches with capped backoff**. A mis-gated document is a visible,
  self-healing annoyance.
- **STAFF pages do NOT retry.** The staff wrapper has no re-attach path, and the file says so
  in its own comment: *"a rejected snapshot listener never retries, so the page stays dead."*
  A mis-gated document there is a permanently blank board for a bidder, with no error they can
  act on and no recovery short of a code push.
**So: staff pages are where the danger lives. Admin pages impose essentially no constraint** —
every admin listener is deferred behind `authReady`, which resolves only inside `_enterAdmin()`
after a verified admin sign-in, and every `getDoc` sits inside a function, not at module load.

**MUST STAY PUBLICLY READABLE — the two staff LOGIN SCREENS block on these before any sign-in:**

| doc | auction staff | schedule staff |
|---|---|---|
| `userList` | roster list · `names.length` | `_namesReadySched` |
| `usernames` | `displayName()` · `_usernamesReady` | `_usernamesReadySched` |
| `adminSettings` | `_rbGateCheck` · `_acCheckRemote` | `_adminReadySched` (as `vAdminRef`) |
| `passcodes` | `_passcodesReady` in `signinReady()` | `_passcodesReadySched` |
| `phases` | `updateSigninPhase` + `biddingOpen()` | not a login-screen dependency |


**✅ BATCH 2 — staff 152, filed 20 Aug 2026; SINCE PUSHED AND LIVE.** Moved
behind sign-in: `locks` · `fteMap` · `bidPhase` · `slots`. **Plan corrections found in the
code:** `mailStats` has NO staff listener (this page only WRITES it via `trackEmailSent`; the
admin listener already sits behind authReady) — so stage 2's listener work is now COMPLETE
except **`timer`, which remains, last and alone** (its callback runs `startCountdownTick()`).
**Owner gave the GO for the timer build, 20 Aug 2026** — the session ended before it started;
the next session runs it as its own smallest-change build (verify startCountdownTick and
renderTimerRulesInfo have no pre-sign-in readers before moving anything).
As with batch 1: prove batch 2 on the live site (owner signs in, board populates) BEFORE
gating any document.

**GATEABLE once its listener moves behind sign-in on the two staff pages** — nothing else reads
them pre-sign-in: `locks` · `changes` · `bestBids` · `bidLowerings` · `bidTimes` ·
`whitelistConfirm` · `fteMap` · `bidPhase` · `slots` · `timer` · `welcomeLog` · `mailStats`.
Two cautions: `timer`'s listener calls `startCountdownTick()` at module level, so moving it
needs care; `whitelistConfirm` is safe because `renderWhitelistBanner` returns early until
`selectedSignInName` is set, which only happens after a real sign-in.

**Method when it is built:** follow the staff page's OWN existing pattern — the post-sign-in
unsub list that already defers `loginEmails` / `emails` / `mailQueue`. Move listeners first,
prove on the live site, THEN gate documents ONE AT A TIME with a battery run between each.
Never a batch. And remember the cost lesson from stage 1: prefer `isVerifiedAccount` (free) over
`isRegisteredUser` (two billed lookups per evaluation) unless a document genuinely needs the
stronger bar.

**STAGE 2 onwards, only after Stage 1 is proven live:** move the twelve candidate listeners onto
the signed-in path following the pattern the staff page ALREADY uses for its sensitive documents
(the post-sign-in unsub list holding `loginEmails` / `emails` / `mailQueue`), then gate those
documents **ONE AT A TIME**, re-running the batteries between each. Never as a batch. The admin
pages need the same login-screen analysis done for them before their reads are gated — it has
NOT been done yet, and assuming they match the staff page would be exactly the kind of unverified
claim §4 warns about.

**Order against FB-4:** do App Check FIRST. It is lower risk, it needs no rules change, and it
removes the outsider threat on its own — which means FB-5 can then be paced carefully instead
of rushed.

## Optional hardening — deferred-minor, no urgency

> **Re-confirmed DEFERRED by the owner, 19 Aug 2026** ("keep deferred"), together with V6,
> C6, C8 and the `phases` document growth item. Nothing in this group is a launch blocker and
> none of it should be started without a fresh explicit "go".

- [ ] Firestore rule freezing `auctionConfig` + `bidFloors` once Phase 1 starts (today the
      Phase-1 lock is client-side; the editor is the only sanctioned write path).
- [ ] Version auto-refresh on tab re-focus (today: page-load only; idle tabs keep the old
      build until next visit — acceptable, documented).

## OVERNIGHT DIRECTIVE — 18 Aug 2026 (owner, going to sleep; answers expected in the morning)

Owner's words: *"Determine what aspects of the site are visually difficult and determine
solutions… Review what excellent scheduling sites do… I am primarily speaking about the
admin scheduling site… The schedule grid, shift eligibility, shift catalog, stats, and
reports section are all hard to read. The shift families is a noticeable standout in that
it looks good and easy to edit. I also want additional ideas on what a fully functional
scheduling site needs with 90 possible shifts, 40 MDs, 40 CRNAs, and working 24/7…
review the layout and code and determine if the site is properly organized… Create a list
of questions for me to answer tomorrow… We are working on a prototype still."*

⚠️ **NEW FACT recorded the turn it was said: the schedule serves 40 MDs AND 40 CRNAs
(~80 people).** Every prior plan assumed the MD-auction roster (~37). CRNAs are not in
`vacations/userList` — where the schedule's CRNA users come from is an open architecture
question. **MORNING ANSWER (owner, 18 Aug): Q1 DEFERRED with principles recorded:**
*"CRNAs may be added in the future"* (not now — the 80-person scale is future, not
immediate); the roster mechanism is *"unclear — we can address that later"*; and the
standing principle: *"Vacation will always be separate, so will need a way to separate
users properly"* — the auctions' rosters and the schedule's users are to be properly
separated when the time comes; no cross-contamination of user lists. Design work that
depends on the roster fork waits; everything else proceeds. **DELIVERED overnight: `schedule/design/UX-PLAN.md`**
— per-section diagnosis + ranked fixes, industry patterns (QGenda/Amion/Lightning Bolt/
Deputy et al.), the missing-features gap list, code verdict (renderAll fan-out and the
three {list:[]} docs are the two real problems; format otherwise sound), a 10-step
proposed sequence, and 13 quick-decision questions (in the plan §8 and in chat).

# 3 · DAILY SCHEDULE — in build, months from use

**State:** see the STATUS block at the top of this file — it is generated, and the numbers that
used to be typed here went stale twice.

> ⚠️ **THE DEFECT LIST BELOW WAS VERIFIED AGAINST BUILD 76/36 AND IS NOW SEVENTEEN BUILDS OLD
> (noted 25 Aug 2026).** Builds 77–93 have shipped since. It is NOT re-verified, and §90 parked
> the defect axis deliberately — the defects that matter cannot be known until someone drives
> the features. **Treat every status below as dated, not current**, exactly as the note at the
> foot of this section already says. Re-verify against the code before acting on any of it.

> 🔎 **THIS SECTION WAS RE-VERIFIED AGAINST THE CODE ON 24 AUG 2026**, build 76/36, after the
> owner asked what was outstanding. The previous text had been carried unchanged since 17 Aug
> and was describing build 63/28 — eight admin builds and eight staff builds out of date. Every
> line below was checked against the shipping bytes by three independent readers; where the
> record and the code disagreed, **the code won and the record was corrected.** Statuses are
> therefore true as of 76/36 and should be re-verified, not trusted, after the next wave.

## Open questions — awaiting the owner

| # | question | why it matters |
|---|---|---|
| Q8 | Phone-first staff view priority — movable stage 8; pull forward if the group starts using the site early. | Sequencing only. **See defect 17 — the admin page has no responsive rule of any kind.** |
| Q10r | **WITH THE OWNER (17 Aug, §57): he corrects the 14 flagged items himself in the Shift Catalog UI.** Stays open until he says done. | Data quality; matters when the schedule goes real. |

## Known defects — **re-verified against build 76/36, 24 Aug 2026**

**Closed since the last pass, verified in the bytes:**
- **29 — HALF CLOSED.** `namesLoaded` became load-bearing in build 66 — it gates the end of boot
  mode in `_flushRender()`. **`schedFteLoaded` is still set and never read**, and that asymmetry
  is live rather than cosmetic: its three sibling flags guard four Users-page writers against
  clobbering unloaded data, and the FTE writers have no loaded-gate at all.
- **23 — CHANGED SHAPE, not closed.** A shift's `role` is no longer purely decorative: it drives
  the Shift Catalog's role filter. It is still read by **nothing that affects scheduling** — not
  eligibility, coverage, stats, the cell editor, or any collision check.

**Open — correctness and data safety:**
- **7 · non-merge overwrites — SPLIT.** `clearMonth` fixed in 59 (batched, v2 marker).
  `clearAllSchedFte` is non-merge but bounded — `dailysched/fteMap` holds only FTE pairs and
  wiping them is the button's stated purpose; the residual is a concurrent per-user FTE write
  lost while the confirm sits open. **`saveBaseline` — ✅ FIXED IN ADMIN 77.** It now counts the
  boxes it expects, using the same id derivation the writer uses, and refuses the whole write if
  even one is missing. **The replace semantics were deliberately KEPT** — the table is the full
  source of truth and a blank box means zero, so `{merge:true}` would have made blanking a value
  impossible. It was NOT the fix, despite being the obvious one. That leaves only defect 7's
  low-stakes tail: two admins editing baselines at the same moment still last-writer-wins.
- **3 · swap apply is not atomic with swap status — ✅ CLOSED IN 123 (28 Aug 2026): one transaction over the swap item and every day document.** The text below is the record of how it stood. The status flip is
  one transaction; the schedule legs are separate follow-on `mutateCell` transactions (up to six
  for a 3-way). No `writeBatch`. Builds 59/61 added *pre*-flip refusals and a failure toast, so
  the window is now surfaced rather than silent — narrowed and instrumented, not closed. The code
  says so itself in a standing comment.
- **30's residual · the `requiredBuilds` ratchet was never ported.** Zero enforcement of any kind;
  the schedule has only the build-50 pull refresher, which reloads but never blocks.
  **⚠️ TRAP FOR WHOEVER PORTS IT:** the schedule's `PAGE` values are `'index'` and `'admin'` —
  *the same keys the auction ratchets*. A naive port would have schedule admin 76 compare itself
  against the auction's admin build and reload-gate forever. It needs its own key namespace and
  its own document, and adding a `vacations/*` read runs into the cardinal rule.
- **9 · security (shape deliberately not described here — these repos are PUBLIC, §3).**
  Becomes fixable as each document migrates; the rules comment at the `dailysched` block is the
  reference. Note §75 defers six schedule-only audit findings; this is that family.

**Open — UX:**
- **17 · mobile — STILL OPEN, and the recorded wording conflates the two pages.** The staff page
  DOES have a small-screen override; the 9px grid IS that override, and it dates to the original
  commit. **The admin page has no responsive rule at all** — desktop 11px, `nowrap`, 44px min
  cells, scrolling off the side of a phone. **Builds 69 and 70 do not help, and 69 slightly
  hurts:** Compact density fixes row height, not the horizontal axis that actually breaks, and it
  moves labels into `title=` tooltips — *tooltips do not exist on touch*. 69 also added another
  9px header row. 70 flips the row axis but leaves one column per day, and is read-only by design.
- **18 · drag-and-drop has no touch fallback — STILL OPEN, narrower than the wording suggests.**
  The only DnD on either page is the Shift Families board (HTML5 DnD, which does not fire on
  touch). **Mitigation nobody is told about:** every Shift Catalog row has a family `<select>`
  calling the same handler, so a touch user can re-parent a shift — they just have to know to go
  elsewhere. The empty column still says "Drop shifts here".

**Open — hardcoding (§11):**
- **20 · two admin e-mail constants — STILL OPEN.** Two byte-identical arrays in the schedule
  admin (the §S11 Admin Access block, and the auth gate). The auction's own third copy is
  deliberately independent and is not part of this defect.
- **21 · MD/CRNA/Both fixed list — STILL OPEN, four hardcoded sites.** Worth noting the code knows:
  the site-seed block explicitly cites this defect as the thing not to repeat. Sites got the
  catalog-with-fallback treatment; role never did. Retires with Stage 4.
- **22 · vacation-site URL — STILL OPEN, exactly one occurrence per page.** No JS-string copies.
- **23 · see CHANGED SHAPE above.**

**Dead code — re-counted, not recalled:**
- **27 · verified, all four claims.** `sha256`, `resolveUser` and `onSignUserInput` are dead
  (note: `resolveByEmail` is a *different*, live function — do not conflate them).
  **`#signWelcomeErr` is referenced by three write sites and must NOT be removed** — but all
  three are *clears*, and nothing ever writes a message into it. It is a permanently-empty error
  surface, which is arguably a missing-message defect rather than a dead-code one.

## Roadmap — **corrected 24 Aug 2026 against the code, not against BUILD-LOG**

| # | stage | state |
|---|---|---|
| 0 | Test battery | **27 suites registered; 6 execute, 21 need Playwright** (see ⚠️ 2). Still missing for auction parity: button sweep, lifecycle run-through, never-events list, fuzzing, mobile sweeps — all five remain auction-only. New gap: builds 71–74 / staff 31–35 (⚠️ 3). |
| 1 | Shift definition | shipped 54–57. **Remaining, unchanged: compatibility checking and group edit (§13)** — two prose strings promise compatibility checking; there is no implementation. |
| 2 | Coverage board | shipped 58, **and extended by 69** — `coverageRows()` gained a per-day roll-up feeding the in-grid coverage strip, so strip and report cannot drift. |
| 3 | Assignment model | shipped 52. Unchanged. |
| 4 | Roles + subgroups | **not started, and still blocked in practice** — zero occurrences of `subgroup`; `role` exists only as a SHIFT attribute; people carry only a display name and `fteMap`. This is why S5c's filter half was shelved. |
| 5 | Rules engine (§44) | **✅ COMPLETE — builds 105–120, 28 Aug 2026.** Seven rule types + two built-in switches (§121), warn at change, quotas as constraints, the ENGINE page (§123) that follows every rule. |
| 6 | Safety net remainder | b61 shipped the unified approval check. **Swap atomicity (defect 3) ✅ CLOSED in 123.** Remaining: uniform confirmations — 40 raw `confirm(` calls and no shared helper (cosmetic; not picked up). |
| 7 | Draft/publish + change feed | **✅ BUILT — 121/staff 38 (draft vs published, §5) and 122/staff 39 (the personal change feed, §6).** Rules-level enforcement of both is an auction deploy (§92), his call. |
| 8 | Staff phone view + notifications | not started — defects 17 and 18 are the concrete evidence. No notification path of any kind. |
| 9 | Reports | shipped 51. Unchanged. |

**Builds since the last time this section was accurate** (71–76 / staff 31–36): App Check on both
pages (71/31) · show the reCAPTCHA badge (72/32) · §66 reversal, hide it again and carry the
required attribution (73/33) · FB-6, the security box moved OUTSIDE both sign-in panels so
returning users see it (74/34) · the retired passcodes listener deleted (staff 35) · the
build-48 loaded-gate on `addSchedUser` (75) · RA-1 fixes — Phase-4 vacation weeks paint once
results are sent, and a failed bid-security-map sync can no longer hide behind a green "Saved"
(76/36).

## Request types — designed, not built

The owner's 27-entry list and both structural rulings live in `design/REQUEST-TYPES.md`
(strength is a field, not three list entries; the requestable list is admin-curated, §41).
Dependencies now real: overnight-call tag ✅ (51) · site on shifts ✅ (54) · a "late" tag ◻ ·
approval checks ✅ (61). Open: the Limits/Balances tabs from the screenshots — not specified,
not invented (§22).

## FUTURE CAPABILITY — owner-raised, written down the turn they are raised

- [ ] **Phone access — CONFIRMED WANTED, 17 Aug 2026, verbatim:** *"They will need to have
      access to the schedule from their phones, either apple or andoird."* Reads as the web app
      working well in both phones' browsers rather than native apps — **ASK before designing.**
      Overlaps stage 8 and defects 17/18. **The admin page has no responsive rule at all today.**
- [ ] **E-mail updating and alerts — CONFIRMED WANTED, same message, verbatim:** *"They will need
      e-mail updating and alerts."* What triggers an alert is unspecified. Shares the auction's
      EmailJS quota — a real constraint to design around.
- [ ] **Calendar feed — APPROVED to design and build, 17 Aug (DECISIONS §54).** NOT released to
      doctors until the owner declares the schedule complete. Build gate open, release gate SHUT.
      Scope settled 17 Aug (§54a): shifts + approved vacations, per-doctor secret link,
      regenerable per doctor. No open questions.
- [ ] **Per-person contact number — WANTED EVENTUALLY, owner 24 Aug 2026: *"It would have to be
      per person for sure, but let's work on that another time."*** Filed here at his request as
      a future item, not a defect and not part of S6 (**§91**). The Day Board ships without it.
      **Settled already, so nobody re-scopes it from scratch:** it is PER PERSON — the per-shift
      role-number alternative (call pager / unit extension) was offered and CLOSED, do not
      re-propose it. It lives in **`dailysched/*`, never the shared roster** (same reasoning as
      FTE, §2 — the auction must not gain a personal-phone field and the sanctioned-writer
      surface must not grow). It **cannot be world-readable**, so it needs a `firestore.rules`
      change, and the rules live in the AUCTION repo — that makes it an auction deploy with
      auction discipline (console publish before dependent client code, RA-2 before and after).
      **It is NOT needed for the calendar feed (§54)** — an `.ics` subscription carries date,
      time, title and location, and the per-doctor secret link is the identity mechanism.
- [ ] **Anything else from the pre-16-Aug sessions** — ask what else he recalls, record each the
      moment he says it.
- The full ideas document is queue item **A8**.
- [ ] **Roster merge — DEFERRED (owner, 18 Aug).** Vacation stays separate; users must be
      separable properly. Revisit at stage-4 rollout or when CRNAs join the schedule.

## Test assets

`tests/sched/`: **suite count comes from the STATUS block at the top of this file** (`status.mjs`
counts the folder); the "27" typed here was stale by 25 Aug. Most execute anywhere; the Playwright
ones need the module, and the battery now says loudly how many DID NOT RUN — a skip is not a pass.
The isolation suite is the cardinal rule's only enforcement — run it before filing anything.
`tests/sched/README.md` is itself stale (it still says 16 suites, build50–build63) and should be
corrected the next time that folder is touched.
Honesty baselines come from git history (see START-HERE's cloud-session notes).

---

*Update discipline: tick the box in the same session the work completes; correct a status the
moment it is known to have changed. Statuses above were executed-verified on 24 Aug 2026 against
build 76/36 — re-verify rather than trust them after the next wave.*
