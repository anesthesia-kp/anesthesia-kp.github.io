# TODO — the ONE list, both sites
<!-- answers: what is OUTSTANDING on both sites — the generated STATUS block, the §1 queue and its standing constraint, the held schedule pile, and the CLOSED / DECLINED never-re-raise table; read at §4 step 1 of every session -->

**Owner ruling, 17 Aug 2026:** one TODO file for the whole project, not four. This is it.
The per-repo `TODO.md` files are pointers here; `MASTER-TODO.md` and
`OPEN-RESIDUALS` were folded in and archived (copies in `_archive/`) the same day.

**What stays separate, and why:** `DECISIONS.md` (this repo) (the rulings register — what is
*settled*) and `schedule/BUILD-LOG.md` (what *shipped*). This file is what is *outstanding*.
A fact lives in ONE of the three, never two.

> ⚠️ **These repos are PUBLIC.** Defects are described by shape, never by reproduction.

<!-- STATUS:BEGIN — generated by status.mjs. DO NOT EDIT BY HAND. -->
**STATUS — generated 2026-09-08 23:36 UTC by `node status.mjs`. Regenerate after every push.**

| | build (disk) | versions.json | live check |
|---|---|---|---|
| Auction admin / staff / mobile | 329 / 169 / — | 329 / 169 / 18 | not checked from here (no network) — fetch live separately |
| Schedule admin / staff | 151 / 51 | 151 / 51 | not checked from here (no network) — fetch live separately |

| repo | working tree | vs origin | git locks |
|---|---|---|---|
| vacation-kp.github.io | clean | in sync with origin | no locks |
| schedule | clean | in sync with origin | no locks |
| tests | 16 uncommitted file(s) | in sync with origin | no locks |
| anesthesia-kp.github.io | 10 uncommitted file(s) | in sync with origin | no locks |

Suites on disk: auction **79** (`tests/test-*.mjs`) · schedule **108**
(`tests/sched/*-test.mjs`). Assertion counts come from RUNNING the batteries, never from here.

🧹 **`_to_delete/` holds 19 files (477 KB) — empty it.** Finder → Documents/GitHub/_to_delete → select all → delete. `_archive/` holds 104 files (11.2 MB) and is KEPT, never emptied.

**Governing files:** START-HERE **164** / 350 · TODO **221** / 700 · HANDOFF **774** / 900 · DECISIONS 5003 (unlimited, indexed 197 rulings) · archives: HANDOFF-ARCHIVE.md 1522 · START-HERE-ARCHIVE.md 816 · TODO-ARCHIVE.md 1699. Over a tripwire = this script exits non-zero = the archive pass is due before hand-over.

**START-HERE.md** — `LAST REVISED 8 Sep 2026`, date checked by git, read in America/Los_Angeles (edited, not yet committed — so it must say today or tomorrow) — ✅ fresh — every build number it quotes matches `versions.json`
_(the only document the owner pastes; this gate exits non-zero when it drifts)_
<!-- STATUS:END -->

---

# 1 · THE QUEUE — work, in order

## ▶ NEXT — NOTHING IS QUEUED ON THE AUCTION. Standing constraint: §92 (no auction change without his specific decision for that change) + §164 (narrow, safe, winding down). Schedule PARKED (§143 / §157).
· **The docs pass (§188) is FILED 8 Sep 2026 — awaiting his push.** Hub repo: the four governing files, three archives, `status.mjs`, `archive.mjs`, `docs-pass-check.mjs`; `tests`: line-2 notes on every `tests/docs/*.md` plus `docs/DOCS-PASS-CHECK.md`. The two site repos are UNTOUCHED (a push there is a deploy; §92) — `status.mjs` carries built-in notes for their `BUILD-LOG.md`s. Gates and the retrieval check are in `HANDOFF.md`'s 8 Sep entry. Delete this line once pushed and `node status.mjs` exits 0 on his Mac.

## 🕓 WAITING FOR A QUIET DAY — the rehearsal's remaining notes (§187: *"when there is no time constraint"* — not before go-live, not urgent). Record: `tests/docs/REHEARSAL-2026-09-07.md`; O-E shipped as admin 329.
| | shape | where |
|---|---|---|
| O-A | Edit Selections: after Cancel or a withheld-number refusal the row's dropdown keeps showing the un-saved value until the table repaints; data untouched | admin |
| O-B | Under Review week header's "Winning" counts machine winners only (Remaining and the reports include approvals) — explained to him | admin, label |
| O-C | Unchecking "Opening window" and saving stores no day count, so re-checking shows 7 days, not the configured 5 (rules save `null`) | admin Controls |
| O-D | Simulator log's first line says "Phase 4 simulation complete" without the round | admin, label |
| O-F | In Phase 4 the simulator counts a user's earlier-round WINS toward "Bids per user", so at target 1 prior-round winners get no new simulated bid (testing tool only) | simulator |

## 🙋 HIS CALL — raised, never ruled. Do not build unasked; do not remind.
· **Three chunks of the admin Dashboard are computed and shown to nobody** (`#dashUsersWrap` / `#dashWeeklyWrap` hard-coded hidden, `exportDashboard` uncalled) — switch on or delete; raised under §148.
· **RA-8's minor tier, unbuilt:** M-4 paint-only lock across a stale second-tab dialog (7 handlers) · M-5 Save All from a stale panel (two admins) · M-6 a both-approved-and-denied user listed twice in the results e-mail (needs a half-failed write) · M-7 dormant dashboard chunks (= the item above) · M-3 the two rehearsal `.docx` files in the site repo still teach "Draw" (the deck is updated, §170). Report: `tests/docs/RA-8-2026-09-01.md`.
· **FB-4's console half:** HTTP-referrer + API restrictions on the browser key (Google Cloud console) — allow the project's `firebaseapp.com` auth domain too or sign-in breaks; test every page after; the CRNA project has its own key. Owner-owned, no code.
· **The calendar-feed token ruling** (where a per-doctor token lives, who may regenerate it) — with the parked feed items below.
· **`_archive/` is not in any git repo** (HANDOFF standing traps) — whether it should be.
· **Claude's own decisions inside 306** (admin keeps the word "tie"; `PROJ_LABEL` keeps its `draw` key on purpose; `outcomeUpperUI`; Weekly Summary one column) — any revert is one change; the list is in `TODO-ARCHIVE.md` ("SHIPPED THIS WEEK").

## 🔔 PHASE-4 EXTRA FTE — AN OWNER TASK BETWEEN PHASES 3 AND 4, NOT A FEATURE (26 Aug)

His table gives 34 weeks (4/18–12/12 except Thanksgiving) **one extra FTE that opens in Phase 4**.
The site has no per-phase capacity feature — checked in code 26 Aug (`getSlots` reads one number
per week) — and he chose the manual route: *"am i correct that i will be able to add an FTE to
those weeks between phases 3 and 4? that would work fine for me."* Confirmed yes, with one
caution from the FTE panel's own dialog: **changing a week's availability re-decides that week's
winners immediately — so raise the 34 weeks only AFTER Phase 3 is completed and its results sent,
never while its bidding is open.** The deck's chart shows these as the amber tops, excluded from
the supply totals by his instruction.

## ⏸ CRNA-SUSPENDED — THE TWO CRNA SITES EXIST BUT ARE OFF THE AIR (owner, 26 Aug evening, §118)

**Addresses (keep):** staff `https://anesthesia-kp.github.io/vacation/crna/` · admin
`https://anesthesia-kp.github.io/vacation/crna/admin/` · Firebase project `crna-vacation`
(config in `vacation-kp.github.io/crna-config.json`). Both serve GitHub's bare 404 while
`"suspended": true` is set there; the landing page has no CRNA cards; reference copies of the
files as they were sit in `_archive/vacation/crna-suspended-2026-08-26/`.

**TO REVIVE (three steps, then two pushes):**
1. `crna-config.json` → `"suspended": false`.
2. `node crna-stamp.mjs` in the auction repo — regenerates `crna/index.html`, `crna/admin/index.html`
   and `crna/versions.json` from the MD pages AS THEY ARE THEN (do not restore the archived copies).
3. Landing page: restore the two `<a class="site-link" …/vacation/crna/…>` cards from
   `anesthesia-kp.github.io` commit `07b001d` (the last commit that had them) — one under the staff
   group, one under "Admin Access Only".
Then run the auction battery (`test-crna-stamp.mjs` flips back to its drift check by itself) and push
`vacation-kp.github.io` and `anesthesia-kp.github.io`. §92 applies: a revival is an auction deploy.

## 📒 THREE BUILD-LOG ROWS ARE MISSING — found 25 Aug, NOT written. Owner's call.

Found while checking whether shipped sections could leave this file. **Each one is why those
three sections STAYED** — the standing rule only lets work leave `TODO.md` once its record exists
elsewhere, and for these it does not.

1. **Schedule build 96** has no row in `schedule/BUILD-LOG.md` (rows run 98, 97, 95, 94, 93).
   It appears only inside the 97 row as an honesty baseline. The commit `16e5977` carries a full
   description, so a faithful row can be written from it — **but that is reconstructing history
   under a build freeze, so it needs his word first.**
2. **The CRNA stamper's 17 Aug "BUILT / HARDENED" work** has no row in either log — the auction
   log starts at build 270 (18 Aug). Later guard versions (v2, v3) DO have rows. The mandate for
   the tool lives inside `DECISIONS.md` §63 and §64, never as its own ruling.
3. **The 18 Aug overnight directive** that produced `schedule/design/UX-PLAN.md` has no row and no
   §. Later builds cite the plan as their source, so the artefact is load-bearing and its
   commissioning is unrecorded. It survives only in this file and `schedule/design/README.md`.

**Recommendation, not a decision:** write rows 1 and 3 from the commits and the artefact, leave 2
alone (the mandate is already ruled). Nothing done unasked.

## 🧪 TEST GAPS — known, unqueued, not defects
· The sweep fixture (`tests/sweep/site/fake/seed.js`) has no completed phase with a denied bid and no Phase-4 rounds: every look-back and round change is proven by executed suites, never by the every-button sweep. Adding one is the highest-value unbuilt thing in that area (1 Sep).
· A completed phase with NO archive (RA-7's F-5 case): the code says the state is unreachable; nothing proves it.
· Any other suite that strips comments may have the `/*`-in-a-line-comment hole `build80` had (START-HERE §3 r10) — not audited.
· `tests/sched/README.md` and `tests/README.md` are stale (suite counts, build ranges) — correct them the next time those folders are touched.

## ⛔ CLOSED / DECLINED — never re-raise, never list as outstanding, never remind him
One line each, with the ruling. An item enters this table the turn he declines it. (`status.mjs` checks every § here exists in DECISIONS.)
| item | ruling |
|---|---|
| Launch, and the sign-in test | OFF the list at his instruction, 25 Aug 2026 — his own; do not re-add, do not remind (START-HERE §1; live state is HANDOFF PART B) |
| His full rehearsal | his own (§169, done §175, and again §186) — do not remind |
| BULK-1 — per-week bulk approve/deny | DECLINED 24 Aug 2026: *"No, I don't want that"*; *"I want both options"* = the existing phase-wide bulk + fast individual clicking (FAST-1, built) |
| M-3, M-6, M-7, M-9 (RA-4 ids) | off the queue by §87 (*"only 100% necessary fixes"*); H-4, H-5, M-8 skipped and M-2, M-5 left alone by §86 |
| M-4 — login e-mail case / padding | CLOSED by his own checks, 22 and 31 Aug (§87; record in `START-HERE-ARCHIVE.md`) |
| The auction-board queue: the abandoned Chrome rehearsal, the unverified board state, the Rehearsal-Mode question, the e-mail-switch finding | CLOSED §146 (closed, not completed) |
| PUSH-ALL, and the Terminal-git credential setup | DROPPED §144 — the script stays on disk unused |
| A suites index in `tests` (to dodge the single-file-commit trap) | DECLINED §157 |
| The schedule — everything on it | PARKED §143 / §157 — pointers only, below |
| The calendar-feed link e-mail and the staff-site link (§137 B) | PARKED §141 — *"not for now"* |
| The welcome e-mails' "Each phase opens with" wording | DECLINED §169 |
| The summer floor and demand calibration | PARKED §176 — *"forget all this for now"* |
| Mid-phase editing of any frozen setting, the cap included | DECLINED §177 |
| A per-user numbers table (3 Sep) | discussed, not asked for (§177 session) |
| Weeks-won vs cap on User Bids | DECLINED §178 |
| RA-10 F7–F13 and F15 (LOW hygiene) | DECLINED §184 — *"skip 3"* |
| A rules-side refusal of held numbers | pushed back in RA-10 (a billed `get()` on every bid write); not ruled, not proposed again |
| The round archive as a second witness for used numbers | not built, his choice §174 |
| "No bids placed in this round yet." after a round is archived | *"that part is fine"* §172 |
| FB-2 — an in-page reads tracker | DECLINED §61 (*"skip the tracker"*) |
| The free-plan reads cliff (FB-1 as a blocker) | RESOLVED §61 — pay-as-you-go, billing alert set |
| The reCAPTCHA badge shown | REVERSED §66 — hidden, attribution mandatory |
| A phone-number field | NO for now §91; if ever, per person in `dailysched/*`; the per-shift role-number alternative is CLOSED |
| The Day Board in its first form | REJECTED §93 |
| "Shift pool" · "demand" | the words are QUOTA (§108) and STAFFING (§103) |
| More sections collapsing by default | NO §115 — the control got the work instead |
| The three 26 Aug choices (107's every-site weekly print; the one-month rules checker; §109) | ACCEPTED as they stand §112 |
| Static `.ics` files in the public repo | REJECTED 26 Aug (public repo, guessable URLs); Functions approved instead §114 |
| The 14-day HANDOFF rule | REPLACED §101 — by relevance, never by age |
| Context thresholds, `ctx.mjs`, a "budgeted close" | KILLED §106 — report the number, never invent a threshold (overruled three times) |
| Roster merge; CRNAs on the schedule | DEFERRED (owner, 18 Aug 2026): vacation stays separate, users must be separable properly; revisit when CRNAs join |
| §158's twelve unchecked surfaces | DO NOT RESTART THE HUNT — §160; unchecked is not clean, a later finding is normal, not a reason to reopen |
| Re-scoping `firestore.rules` for M-4 padding | inside §92; not for this |

---

# 2 · VACATION AUCTION — live, in production

## Accepted-for-launch residuals (the 25 Jul code-review ids — never quote a bare id; full sketches in `tests/docs/CODE-REVIEW-FINDINGS.md`; the 19 Aug analyses in `TODO-ARCHIVE.md`)
| id | what | status | neutralised by |
|---|---|---|---|
| **M1** | results e-mails can double-send if two admins press Send inside a slow >2-min window | ACCEPTED for launch, 18 Aug 2026 (his July ruling: *"better to leave it alone than create new problems"*) | one admin runs the auction |
| **L2** | mail-queue claim is claim/settle/re-check, not a transaction | ACCEPTED for launch, 18 Aug 2026 | worst case one duplicate alert from a lagging background tab |
| **L3** | a bid queued on an offline device can replay into a freshly reset auction, stale phase tag, no error | ACCEPTED for launch, 18 Aug 2026 | after Reset: Global Lock ON until Begin Phase 1; glance at the board before going live |
| **L4** | an earlier phase's unsent results can become unsendable (narrowed in 245) | ACCEPTED for launch, 18 Aug 2026 | send or rehearsal-skip each phase's results before beginning the next |
| **FB-1** | reads on a busy deadline day (one bid = 3–5 listened documents × connected clients) | WATCH — the live rehearsal peaked at ~15,000 reads/day, Phase 4 never measured; §61 made an overrun cents, not an outage | re-check the console Usage graph after Phase 1 of the real run |
| **FB-3** | the schedule shares the project and the bill; 19–25 listeners per page, every working day once live | no impact today | **redo the FB-1 arithmetic BEFORE the schedule goes live** — nothing guards shared spend |

## Watch items — dated facts, not defects

- [ ] **The `phases` document grows ~20KB per Phase-4 round against Firestore's 1MB cap.**
      Late 2027 on current rates. Detail: `HANDOFF.md` D3.

## Deferred by him — nothing here starts without a fresh, explicit "go"
· **V6 · batch-add users** — approved 18 Aug, then deferred the same day (*"we can defer user list changes to later"*). Spec, verbatim: one user per line, order-free fields (initials, full name, gmail, FTE, kp e-mail), *"only name is a mandatory field to add"*; parser by shape (kp.org → KP; other e-mail → Google login; 0–1 → FTE; short all-caps → initials; rest → name), missing initials proposed in a PREVIEW, create/update/rejected preview before any write (the build-56 pattern), per-line duplicate-login refusal, sanctioned save paths only; built ONCE on the MD admin. Full text in `TODO-ARCHIVE.md`.
· **C8 · scheduling links on the CRNA admin** — removed by the stamper (owner 18 Aug: *"for now, they do not belong there"*); decide deliberately when the schedule goes live. **C6** — a separate EmailJS account for CRNA, the MD site label, CRNA rows in the capacity report: later, his call. (The CRNA sites are SUSPENDED — §118, recipe above.)
· **FB-5 · read-privacy, what is left — UNVERIFIED against current bytes.** Done: RA-3's hardening (`changesArchive`, `welcomeLog` — START-HERE §1 fact 4) and staff 152 (`locks` · `fteMap` · `bidPhase` · `slots` behind sign-in). Per the 19–20 Aug plan the `timer` listener move (his GO, 20 Aug — the record shows it never started) and gating each moved document ONE AT A TIME were next, and the admin pages' login-screen analysis was never done. Staff pages never retry a denied listener, so a mis-gated document there is a dead board. §92 applies; the full analysis (three FB-5 sections, the enforcement checklist, the App Check readings) is in `TODO-ARCHIVE.md`.

## Optional hardening — deferred-minor, no urgency

> **Re-confirmed DEFERRED by the owner, 19 Aug 2026** ("keep deferred"), together with V6,
> C6, C8 and the `phases` document growth item. Nothing in this group is a launch blocker and
> none of it should be started without a fresh explicit "go".

- [ ] Firestore rule freezing `auctionConfig` + `bidFloors` once Phase 1 starts (today the
      Phase-1 lock is client-side; the editor is the only sanctioned write path).
- [ ] Version auto-refresh on tab re-focus (today: page-load only; idle tabs keep the old
      build until next visit — acceptable, documented).

---

# 3 · DAILY SCHEDULE — PARKED (§143 / §157). The held pile, as pointers.
Nothing here is picked up unasked. Schedule work stops the moment the auction needs attention (START-HERE §1); §92 closes `firestore.rules`. The full design record (RA-6 and its wave, Stage 5, quotas, the collapse brief, the 18 Aug UX directive) is in `TODO-ARCHIVE.md` and `tests/docs/RA-6-2026-08-28.md`.

## Open questions — awaiting the owner

| # | question | why it matters |
|---|---|---|
| Q8 | Phone-first staff view priority — movable stage 8; pull forward if the group starts using the site early. | Sequencing only. **See defect 17 — the admin page has no responsive rule of any kind.** |
| Q10r | **WITH THE OWNER (17 Aug, §57): he corrects the 14 flagged items himself in the Shift Catalog UI.** Stays open until he says done. | Data quality; matters when the schedule goes real. |

## The pile — one line each, with its ruling
· **S-7** (the "specific shift" request option once his request-type list is final) · **B-15** (a comment in `firestore.rules`, §92) — LATER by §142, held by §143.
· **The calendar feed** (LIVE and verified end to end, §136–§140; release gate §54 SHUT): the link e-mail (admin 150) needs a RECHECK before any real send — the SUBJECT lives in the EmailJS template `template_rss3fn3`, shared with the auction's alerts: press E-mail on his own row and read what arrives; the staff-site link (§137 B) needs a per-doctor readable token — a §92 rules diff first. Both PARKED §141. A separate sender address for the schedule (owner 31 Aug: *"vacation goddess isn't a good one for scheduling"*) — held; quota note in START-HERE §2.
· **Claude's §131/§132 decisions on his behalf** (the sidebar regrouped under his §133 nouns; holidays stay on the catalog page; the Reports/call-baseline folds open shut; eligibility opens on the first family with a count line; the cell editor has ✕ and Escape, no Cancel; a swap denial's reason is optional; Publish tells a whole-month-off person; a recolour onto a used colour asks; S-4 left alone; R-11's guard reordered) — any revert is one change; the reasons are in `TODO-ARCHIVE.md` (RA-6 section).
· **§7b "what a plan would do to existing assignments"** — Claude recommends against (E-1 is fixed; it adds a panel to a page just simplified).
· **Stage 5 leftovers:** the rules checker sees ONE month (§112 accepted the limit; reading the adjacent month is a read-cost decision) · uniform confirmations — 40 raw `confirm(` calls, cosmetic · **[103b]** `max` absorbing the per-shift `capacity` (two fields meaning one thing; also live in the cell editor's "2/3 filled" label and the swap warning) — whether built is UNVERIFIED: read the code before acting.
· **Rules-level enforcement** of the draft gate and the change feed (`dailysched/changes`) — §92, his; today both are enforced only in the pages. A `requiredBuilds` gate for the schedule (defect 30's residual) needs its own key namespace and document — the auction's keys collide.
· **From §44's owner-picked set, not seen built:** who's-on-now · open-shift claim alerts · holiday multi-year equity — verify in code before scoping.
· **Owner-raised, unscheduled:** phone access (*"either apple or android"* — the web app on phones; ASK before designing; staff 50 did the phone layout) · e-mail updating and alerts (triggers unspecified; shares the auction's EmailJS quota) · per-person contact number (§91: per person, in `dailysched/*`, needs a rules change — an auction deploy) · roster merge and CRNAs on the schedule (deferred, 18 Aug; *"Vacation will always be separate"*) · anything else from the pre-16-Aug sessions — ask what he recalls, record it the moment he says it · the ideas document is queue item A8.
· **Request types:** the 27-entry list and both structural rulings in `schedule/design/REQUEST-TYPES.md` (strength is a field; the requestable list is admin-curated, §41); the Limits/Balances tabs from his screenshots are not specified, not invented (§22).
· **Known defects, dated 24 Aug 2026 (build 76/36) — re-verify in code before acting on any:** 7's tail (two admins editing baselines at once, last writer wins) · 9 security (shape not described — public repo; the rules comment at the `dailysched` block is the reference) · 17 the ADMIN page has no responsive rule at all · 18 the Shift Families drag-and-drop has no touch fallback (the catalog row's family `<select>` is the unadvertised route) · 20 two byte-identical admin e-mail arrays · 21 the MD/CRNA/Both fixed list (four sites; retires with Stage 4) · 22 the vacation-site URL once per page · 27 dead code (`sha256`, `resolveUser`, `onSignUserInput`; `#signWelcomeErr` is cleared by three sites and written by none) · 29 `schedFteLoaded` is set and never read while its siblings gate writers · 30 `requiredBuilds` never ported. The full statuses and the roadmap table are in `TODO-ARCHIVE.md`.
· **Test assets:** suite counts come from the STATUS block; the Playwright suites need the module (START-HERE §6); `isolation-test.mjs` is the cardinal rule's only enforcement — run it before filing anything.

---

*Update discipline: tick the box in the same session the work completes; correct a status the
moment it is known to have changed. Statuses above were executed-verified on 24 Aug 2026 against
build 76/36 — re-verify rather than trust them after the next wave.*
